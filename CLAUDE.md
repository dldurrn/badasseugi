# 받아쓰기 공책 — 개발 지침

초등 저학년 자녀를 위한 받아쓰기·맞춤법 학습 웹앱입니다.
부모가 문제를 넣어 주고, 아이가 혼자 듣고 풀고 채점받고, 부모는 리포트로 확인합니다.

---

## 절대 원칙 — 임의로 바꾸지 마세요

이 항목들은 기획 단계에서 여러 차례 논의해 확정한 것입니다.
구현 편의를 위해 바꾸면 제품의 의도가 무너집니다.

1. **보상은 최종 결과에만.** 문제를 푸는 도중에는 축하 연출을 넣지 않습니다.
   시험 모드를 끝까지 마쳤을 때만 카드(100점)·배지(90점 이상)를 줍니다.
   연습 모드에는 보상이 없습니다.
2. **끝까지 마쳐야 기록에 반영됩니다.** 중도 이탈 시 점수·오답노트·보상 모두 저장하지 않습니다.
   나가기 전 확인 창을 띄웁니다.
3. **제출 전 최종 확인 단계.** "확인"을 누르면 바로 채점하지 않고,
   입력값을 보여주고 "다시 고치기 / 제출하기"를 한 번 더 거칩니다.
4. **입력창의 브라우저 맞춤법 교정을 끕니다.**
   `spellCheck={false}` `autoCorrect="off"` `autoCapitalize="off"`
   빨간 물결 밑줄이 뜨면 정답을 미리 알려주는 셈입니다.
5. **오답노트 졸업은 "연속 2회". 날짜는 따지지 않습니다.**
   두 번 잇달아 맞히면 졸업입니다. 오늘 안에 끝낼 수 있어야
   오답노트가 밀린 숙제가 아니라 치울 수 있는 목록이 됩니다.
   두 번을 요구하는 이유는 시간 간격이 아니라 **찍어서 맞힌 것을 거르기 위해서**입니다.
   (2지선다를 연속 두 번 맞힐 확률은 25%)
   한 번이라도 틀리면 별은 0으로 돌아갑니다.

   **두 번째는 같은 문제가 아니라 「짝 문제」입니다**(`src/lib/twin.ts`).
   같은 문장을 두 번 내면 **찍기는 걸러도 암기는 못 거릅니다** —
   두 번째쯤이면 규칙을 익힌 게 아니라 그 문장을 외운 것일 수 있습니다.
   그래서 같은 규칙의 다른 문제를 냅니다. 별 두 개도, 연속도, 0으로 돌아가는 것도 그대로라
   **원칙을 깨는 게 아니라 원칙이 적어 둔 목적에 더 가까워지는 것**입니다.
   짝을 못 만들었으면 예전처럼 원본을 두 번 풉니다.

   > 예전에는 "서로 다른 날짜"를 요구했습니다. 같은 날 반복은 단기기억이라는 이유였는데,
   > 아이가 오늘 아무리 해도 진도가 안 나가는 쪽의 손해가 더 컸습니다. 이 전제는 버렸습니다.
6. **채점 결과는 위아래 정렬 비교로.** 내가 쓴 글자와 정답 글자를 같은 세로줄에 놓습니다.
   두 줄을 따로 그리면 길이가 달라 어긋나서 비교가 안 됩니다.
   띄어쓰기 오류는 ∨(띄어요) / ✕(붙여요) 표식으로 위치를 짚어 줍니다.
7. **아동 개인정보 최소화.** 자녀 실명을 받지 않습니다. 별명만 씁니다.
   계정은 부모 이메일로만 만듭니다. (만 14세 미만 아동 정보 처리 시 법정대리인 동의 요건)
8. **API 키는 절대 클라이언트에 노출하지 않습니다.**
   OCR·TTS 등 외부 API는 반드시 서버 라우트를 경유합니다.
9. **부모 전용 화면(문제 세트 만들기, 리포트)은 자녀 화면에서 막습니다.**
   메뉴에서 감추는 데 그치지 않고, 주소를 직접 쳐도 화면이 열리지 않습니다 —
   「보호자 화면에서 볼 수 있어요」와 「프로필 바꾸기」 버튼이 대신 나옵니다.
   막으려는 것은 리포트 엿보기가 아니라 **정답 문장 열람**입니다 —
   보호자 화면에서 세트를 열면 정답이 그대로 보이니까요.

   **PIN은 자녀 프로필에 들어갈 때 묻습니다**(선택 사항, 안 걸면 그냥 들어갑니다).
   형이 동생 기록에 들어가는 것을 막는 용도입니다.
   **보호자 화면으로 넘어갈 때는 묻지 않습니다** — 화면을 감추는 정도로만 나눕니다.
   그 밖의 엄격한 서버 권한 검증은 범위 밖입니다.

   > 예전에는 이 항목이 "보호자 화면으로 넘어갈 때 PIN을 묻는다"고 적혀 있었는데,
   > 아래 인증 절에는 "보호자 화면 전환에는 잠금이 없습니다"라고 적혀 있었습니다.
   > 실제 동작은 후자입니다. 실제에 맞춰 고쳤습니다.
10. **쓰기 편한 쪽을 고릅니다. 코드가 조금 복잡해지는 것은 감수합니다.**
    구현이 단순하다는 이유로 손이 더 가는 화면을 만들지 않습니다.
    걸음 하나를 줄이거나 화면 왕복 하나를 없앨 수 있다면 그쪽을 택합니다.

    다만 **품이 크게 들거나 나중에 고치기 어려워질 것 같으면 먼저 알립니다.**
    무엇을 얻고 무엇을 잃는지, 더 싼 대안이 있는지 함께 적어 판단을 넘깁니다.
    말없이 어려운 길로 들어가지도, 말없이 쉬운 쪽으로 타협하지도 않습니다.

---

## 이미 구현되어 있는 것 (건드릴 때 주의)

### 채점 엔진 — `src/lib/grading.ts`
**전체 292개 테스트가 통과한 상태입니다. 수정하면 반드시 `npm test`로 확인하세요.**

- 편집거리(Levenshtein) 정렬로 정답과 입력을 글자 단위로 맞춥니다.
- 역추적 시 대체(sub)를 우선해서 같은 자리 글자끼리 짝지어집니다.
  이게 깨지면 "받침만 다름"을 감지하지 못합니다.
- 오류 유형 6가지: `batchim` `vowel` `consonant` `spacing` `punct` `letter`
- `columns` 배열이 화면 렌더링의 단위입니다.
  각 열은 위(내가 쓴 것)/아래(정답) 한 쌍이거나, 띄어쓰기 표식입니다.
- **공백은 글자와 맞바꾸지 않습니다**(`subCost`).
  편집거리에 그냥 맡기면 값이 같은 길이 둘 생기고, 대체를 우선하는 역추적이 엉뚱한 쪽을 고릅니다.
  「작은 발자국」에 아이가 「자근발자국」이라고 쓰면 **「자」가 「은」 밑에** 놓였습니다 —
  ∨ 표식이 사라져 아이가 띄어쓰기를 틀렸다는 것조차 몰랐고,
  유형이 `letter`로 적혀 오답노트·리포트·짝 문제까지 잘못 겨눴습니다.
  맞바꿈 값을 3으로 두면 지움+넣음(늘 2 이하)이 언제나 싸서 **뽑힐 수가 없습니다.**
  2로 두면 동점이 되어 도로 대체가 뽑히니 3이어야 합니다.
- **같은 글자가 이어지면 표시를 뒤쪽으로 밉니다**(`shiftGapsRight`).
  「접다..」에서 두 점 중 어느 것이 군더더기인지 편집거리는 가리지 못합니다.
  앞엣것을 짚으면 바르게 찍은 점이 틀렸다고 나오고 덧붙인 점은 맞았다고 나옵니다.
  DP와 역추적은 건드리지 않고, 정렬이 끝난 뒤 `columns`만 다듬습니다.

검증 방식:
- `grading.test.ts` — 48개 구체 케이스 (한글 11,172자 전체 왕복 검증 포함)
- `grading.fuzz.test.ts` — 무작위 2,500회 속성 기반 교차 검증
- `grading.spacing.test.ts` — 띄어쓰기가 글자 오류로 둔갑하지 않는지 12개
- `wongoji.test.ts` — 원고지 칸 규칙·왕복·칸 이동 81개
- `review.test.ts` — 별·졸업 규칙, 짝 문제의 두 걸음 26개
- `twin.test.ts` — 짝 고르기·거르기·난이도 39개
- `spelling-bank.test.ts` — 문제은행 무결성·난이도 분포 19개
- `dictation-bank.test.ts` — 단계 구조·문장 품질 14개
- `pin.test.ts` — PIN 해시·프로필 전환 판정 12개
- `sets.test.ts` — 세트 입력 정규화, 한국 시간 날짜·주 계산 14개
- `auth-messages.test.ts` — 가입·로그인 오류를 원인별로 다르게 안내하는지 15개
- `grade-cells.test.ts` — 원고지 채점표의 칸 수가 쓰던 원고지와 같은지 22개
- `coach.test.ts` — 짚어 줄 곳·말풍선 자리·안내 문구가 제품 원칙과 맞는지 24개
- `install.test.ts` — 실제 UA 로 기기를 가리고 갈래마다 맞는 안내를 내는지 19개
- `report.test.ts` — 갈래를 섞지 않는지, 안 한 주와 0점을 가리는지 13개
- `wrong-events.test.ts` — 틀린 것만 쌓는지, 복습을 갈라 두는지 10개
- `tts-engines.test.ts` — 공급자 넘어가기·회사 고르기·하루 한도 상한·목소리 옮기기 24개
- `settings.test.ts` — 아이 것이 부모 기본값을 덮되 안 고른 것은 따라가는지, 회사 고르기 24개
- `plan.test.ts` — 무료/유료 한도가 아직 아무도 막지 않는지 9개

### 원고지 — `src/lib/wongoji.ts`, `src/components/Wongoji*.tsx`
아이가 학교 공책처럼 **칸마다 한 글자씩** 씁니다(한 줄 15칸).
**보호자 설정**에서 「그냥 쓰기」로 바꿀 수 있습니다 — 세션 화면에는 두지 않습니다(아래 음성 절).

**원고지는 보이는 방식일 뿐이고, 저장·채점은 언제나 원래 문장으로 합니다.**
쉼표 때문입니다 — 원고지에서 쉼표 뒤는 한 칸을 비우지 않는데 문장에는 공백이 있습니다.
격자를 그대로 이어 붙이면 "호로록,한 입"이 되어 **바르게 쓴 아이가 틀렸다는 채점을 받습니다.**

| 무엇 | 언제 |
|---|---|
| `toCells` | 정답·미리보기 — 원고지 규칙 적용(쉼표 뒤 공백 흡수) |
| `toWritingCells` | **아이가 쓰는 중** — 다듬지 않고 누른 대로 |
| `toText` | 칸 → 문장. 쉼표·마침표 뒤 공백을 되살립니다 |
| `writingToText` | **제출 직전 반드시 거칩니다.** 「그냥 쓰기」에서는 부르면 안 됩니다 |

**쓰기·확인·채점 세 화면이 모두 원고지입니다.** 가운데(확인 단계)만 한 줄짜리 글이었는데,
하필 거기가 「내가 쓴 게 이거 맞나?」를 묻는 자리라 견줄 것을 다른 모양으로 내밀고 있었습니다.
쉼표를 규칙대로 붙여 쓴 아이가 공백이 되살아난 문장을 보고 「다시 고치기」를 눌렀습니다.

격자가 담긴 상자를 꽉 채워야 하는 자리에는 `fill` prop을 줍니다(`.wg--fill`).
안 주면 528px에서 멈춰 라벨·버튼과 세로선이 어긋납니다 — 기둥이 둘로 보입니다.

`toCells`를 쓰는 화면에 그대로 쓰면 끝의 공백이 잘려 **스페이스가 안 먹는 것처럼 보입니다.**

입력은 격자 위에 투명하게 덮은 **입력칸 하나**가 받습니다. 칸마다 입력칸을 두면
`ㅇ → 여 → 예` 조합이 칸 경계에서 끊기고, 그 동작이 키보드 앱마다 다릅니다.

**그 입력칸은 속으로 한 줄짜리라, 「줄」이 필요한 동작은 전부 손으로 되짚어야 합니다.**

| 무엇 | 그냥 두면 | 그래서 |
|---|---|---|
| 누른 자리 | 둘째 줄을 눌러도 첫째 줄로 | `cellToTextOffset`으로 커서를 다시 잡음 |
| ↑ ↓ | 맨 앞·맨 끝으로 튐 | `cellByRow`로 15칸씩 |
| Home / End | 글 전체의 처음·끝 | `rowStart`·`rowEnd`로 그 줄 안에서만 |
| 지우개 | 언제나 **앞 칸**을 지움 | `eraseCell`로 **짚은 칸**을 지움 |

지우개가 제일 어긋났습니다. 종이 원고지에서는 칸을 짚고 지우면 그 칸이 지워지는데,
입력칸의 백스페이스는 앞 칸을 지웁니다 — 「학꾜에」의 「꾜」를 고치려고 짚고 지우면 「학」이 사라집니다.
**짚은 칸에 글자가 있을 때만** 바꿔치기하고, 끝에서 이어 쓰는 중이면 원래 동작에 맡깁니다.

**조합 중(`isComposing`)에는 어떤 키도 가로채지 마세요.**
조합 중 백스페이스는 「각 → 가 → ㄱ」처럼 자모를 하나씩 떼는 한글 고유 동작입니다.

커서 칸은 **포커스가 있을 때만** 진하게 칠합니다(`wg-cell--here`) — 없으면 옅게(`wg-cell--cursor`).
언제나 같은 세기로 칠했더니 지금 쓰는 중인지 그냥 표시인지 구분이 안 됐습니다.

**넓히는 것은 격자가 아니라 화면 전체입니다**(`.page--write`, 안쪽 720px).
오답노트에서 그 자리 풀기도 같습니다 — `.note--writing`이 **카드째** 넓힙니다.
좁은 카드에서는 칸이 18px, 글자가 하한(14px)에 걸려 칸의 77%를 채워 끼었습니다.
15칸이라 격자의 폭이 곧 글자 크기라 넓혀야 하는데,
격자만 밀어내면 라벨·버튼·채점표가 그대로 있어 **기둥이 두 개로 보입니다** —
「들은 문장을 써 보세요」가 격자보다 안쪽에서 시작하니 무엇을 가리키는 말인지 흐려지고,
오답노트 안에서는 격자가 카드 밖으로 삐져나옵니다.

「그냥 쓰기」는 넓히지 않습니다. 한 줄짜리 입력칸이 720px이면 눈이 되돌아오기 힘듭니다.

글자 크기는 화면(vw)이 아니라 **격자**를 따라갑니다(`container-type: inline-size` + `cqw`).
화면 폭으로 재면 같은 화면 안에서도 좁은 기둥 — 오답노트 카드 — 에서 글자가 칸에 꽉 낍니다.
어디서든 칸의 70~73%입니다.

| 화면 | 기둥 | 칸 | 글자 |
|---|---|---|---|
| 데스크탑·세로 태블릿 | 720 | 47 | 33 |
| 폰(세로) | 355 | 23 | 16 |
| 오답노트에서 풀 때 | 720 (카드째 넓힘) | 45 | 31 |

**채점표는 아이가 쓴 방식대로 보여 줍니다** — 원고지에 썼으면 칸으로, 그냥 썼으면 글로.
없는 격자를 채점에서 처음 만나면 무엇을 보라는 건지 한 박자 늦게 알아듣습니다.

**어느 쪽이든 「내가 쓴 것」과 「정답」은 같은 세로줄에 놓습니다**(절대 원칙 6).
벗기는 것은 격자이지 짝이 아닙니다. 두 줄을 따로 흘려 쓰면
글자를 빠뜨렸을 때 길이가 달라져 어긋납니다.

| | 원고지 | 그냥 쓰기 |
|---|---|---|
| 그리는 함수 | `GradePaper` | `GradeLine` |
| 쓰는 값 | `toGradeCells(columns)` — 칸에 앉힘 | `columns` 그대로 |
| 격자·남은 종이 | 있음(한 줄 15칸) | 없음. 문장 끝에서 끝남 |
| 쉼표 뒤 공백 | 흡수(원고지 규칙) | 그대로 — **원고지 규칙이니까요** |
| 띄어쓰기 | 빈 칸으로 드러남 | ∨ ✕ 표식 |

**두 벌이지만 데이터는 한 벌입니다.** 열(`columns`)을 칸으로 옮기는 셈은 원고지 쪽에만 있습니다.
예전에 두 벌이 짐이 됐던 것은 그 셈이 두 벌이었기 때문입니다.

원고지 쪽은 **격자 하나가 한 줄(15칸)**이고 그 안에 위/아래 두 줄이 들어갑니다.
전체를 한 격자에 이어 붙이면 「내가 쓴 것」이 다 지나간 뒤에야 「정답」이 나와
위아래로 견주는 뜻이 사라집니다.

그냥 쓰기 쪽은 **모든 열의 키를 못 박습니다**(`.gl-mine` 30px / `.gl-ans` 28px).
안 그러면 ∨ 표식이 든 열만 키가 달라 표식이 글자보다 낮게 앉고,
「윗줄 = 내가 쓴 것」이 한 줄로 안 읽힙니다.

> 한때 원고지 하나로 합쳤던 적이 있습니다. 합친 까닭은 「몇째 칸에서 틀렸는지 세려면
> 칸이 이어져 있어야 한다」였는데, 그건 **원고지에 쓴 아이 얘기**였습니다.
> 그냥 쓰기로 쓴 아이에게는 셀 칸이 애초에 없습니다.

**칸 세는 법이 쓰는 화면과 같아야 합니다.** 특히 쉼표 뒤 —
채점에 넘기는 문장에는 공백이 있어서 그냥 그리면 칸이 하나 더 생깁니다.
`absorbsFollowingSpace()`를 `toCells`와 함께 씁니다. 테스트가 내장 문제 전부를 지킵니다.

띄어쓰기 오류는 원고지에서 **칸 자체**로 드러납니다.
| 칸 | 윗줄 | 아랫줄 |
|---|---|---|
| `blank` 맞게 띄움 | 빈 칸 | 빈 칸(초록) |
| `needSpace` 안 띄움 | ∨ (빨강) | 빈 칸을 초록 테로 짚음 |
| `extraSpace` 더 띄움 | ✕ (빨강) | 정답에 없는 칸(점선) |
| `pad` 문장 끝난 뒤 | 남은 종이 | 남은 종이 |

### 인증과 프로필 — `src/proxy.ts`, `src/lib/profile*.ts`, `src/lib/pin.ts`
프록시(Next 16에서 middleware가 proxy로 바뀌었습니다)가 문 두 개를 지킵니다. **로그인했는가 → 누가 쓰는지 골랐는가.**
둘 중 하나라도 아니면 `/login` 또는 `/children`으로 보냅니다.
`/api/*`는 리다이렉트하지 않습니다. 화면이 아니라 fetch 응답이라, 각 라우트가 401 JSON을 돌려줍니다.

지금 누가 쓰는지는 쿠키 두 개로 기억합니다. **httpOnly라 화면 코드에서는 못 읽습니다.**
자녀 id가 필요한 화면에는 서버 컴포넌트가 props로 내려주세요 (`readActiveChild()`).

| 쿠키 | 뜻 |
|---|---|
| `bs_view` | `parent`(보호자 화면) / `child`(자녀 화면) / 없음(아직 안 고름) |
| `bs_child` | 마지막에 고른 자녀. 보호자 화면에서도 남습니다(리포트 기본값용) |

PIN은 `src/lib/pin.ts`의 scrypt로 **서버에서만** 다룹니다.
`children.pin_hash`는 부모 RLS로 읽히기 때문에, 화면에서 비교하면 해시가 브라우저로 내려갑니다.
그래서 `/api/children/[id]/enter`에서만 검증하고, 화면에는 `hasPin` 불리언만 보냅니다.
`PIN_LENGTH`·`isValidPin`은 `profile.ts`에 있습니다 — `pin.ts`는 node:crypto를 쓰므로 화면에서 불러올 수 없습니다.

보호자 화면 전환에는 잠금이 없습니다. 지침 9대로 화면을 감추는 정도로만 나눕니다.

로그인 전에도 열리는 화면은 `/login` `/auth` `/privacy` `/terms` 입니다.
약관과 방침이 여기 있는 것은 **가입 화면에서 링크로 걸리기 때문**입니다 —
읽으라고 걸어 놓고 누르면 로그인으로 돌려보내면 말이 안 됩니다.

### 복습 규칙 — `src/lib/review.ts`
`applySessionOutcomes()`가 별·졸업을 판정합니다. **세션을 끝까지 마쳤을 때만 호출하세요.**
같은 세션에 같은 문제가 여러 번 나와도 한 번만 반영합니다(하나라도 틀리면 틀림).

**두 번째 걸음은 짝 문제입니다** — `src/lib/twin.ts`.
지금 원본을 푸는지 짝을 푸는지는 `stepOf()`가 정하는데, **칸을 따로 두지 않습니다.**
`streak`이 이미 답을 갖고 있습니다(0이면 원본, 1이면 짝). 상태를 두 군데 두면 언젠가 어긋나고,
어긋나면 아이가 같은 문제를 두 번 풀거나 원본을 건너뜁니다.

`ItemOutcome.refId`는 **짝을 풀 때도 원본의 것**입니다.
짝의 것을 보내면 그것이 새 오답노트가 되어 목록이 끝없이 불어납니다 —
짝은 그 노트의 두 번째 걸음이지 별개의 문제가 아닙니다. 테스트가 이걸 지킵니다.

**짝에서 틀리면 그 짝을 버립니다.** 남겨 두면 다음 판에 같은 짝이 또 나와
아이가 원본도 외우고 짝도 외웁니다. 원본에서 틀렸을 때는 짝이 아직 안 쓰였으니 그대로 둡니다.

날짜는 별 판정에 쓰지 않습니다. `lastCorrectDate`는 지금도 기록하지만
"오늘 별을 받았어요" 같은 안내용입니다. `notesEligibleToday()`는 의미가 없어져 없앴습니다 —
아직 졸업하지 않았으면 언제든 별을 받을 수 있으므로 `activeNotes()`를 쓰세요.

### 음성 — `src/lib/tts.ts`, `tts-app.ts`, `tts-engines.ts`
층이 셋입니다. **화면은 어느 회사 소리를 쓰는지 모릅니다.**

```
화면  →  appSpeech (tts-app.ts)  →  /api/tts  →  tts-engines.ts  →  타입캐스트 / Google
                 ↓ 안 되면
           브라우저 내장 음성
```

**어느 회사를 쓸 수 있는지는 환경 변수가 정합니다.** `TYPECAST_API_KEY`가 있으면 타입캐스트가 목록에 듭니다.
**키를 지우면 곧바로 되돌아갑니다** — 회사별 차이는 `tts-engines.ts` 한 파일에 갇혀 있습니다.

**둘 다 꽂혀 있으면 부모가 고릅니다**(보호자 설정 · `families.default_engine`).
자동 / Typecast / Google 셋이고 기본은 자동입니다.
**하나만 꽂혀 있으면 고를 거리를 아예 안 그립니다** — 눌러도 안 바뀌는 버튼을 만들지 않습니다.

집 단위입니다. 아이마다 다른 회사를 쓸 까닭이 없고, 이건 학습 취향이 아니라
집의 선택(소리 질과 비용)입니다. 그래서 `children` 에는 칸을 두지 않았습니다.

**골라도 그 회사만 고집하지는 않습니다.** `pickEngines(preferred)` 는 **순서만 바꿉니다.**
「Google 로 들을래요」는 소리의 취향이지 「안 되면 소리 없이 두세요」가 아닙니다.
걸러 내면 그 회사가 막힌 날 아이가 받아쓰기를 못 합니다.

고른 값은 **서버가 읽습니다**(`readEnginePref()`). 화면이 보낸 값을 쓰지 않습니다 —
지금은 어느 쪽이든 우리 키로 우리 소리를 내는 것이라 위험할 게 없지만,
나중에 「무료는 Google」 같은 규칙이 생기면 화면이 정하게 둔 자리가 그대로 구멍이 됩니다.

회사를 바꾸면 **담아 둔 소리를 버리고 한 문장을 읽어 줍니다.**
안 버리면 예전 회사가 만든 소리가 그대로 나서 눌러도 안 바뀐 것처럼 들립니다.
그리고 이 설정의 요점이 「어느 쪽 소리가 나은가」라, 들어 보지 않으면 고를 수가 없습니다.

**한 회사가 막히면 다음 회사로 넘어갑니다**(`pickEngines()`가 목록을 돌려줍니다).
아이 화면에서 **소리가 나는 것**이 어느 회사 소리냐보다 중요합니다.
예전에는 회사 하나만 고르고 끝이라, 타입캐스트가 403으로 막히자
Google 키가 멀쩡한데도 브라우저 내장 음성으로 떨어져 며칠이 그대로 갔습니다.
막힌 회사는 10분간 건너뜁니다(`markBlocked`) — 문장마다 헛걸음하면 아이가 그만큼 기다립니다.

**목소리는 회사가 무엇이든 여자·남자 둘만** 냅니다(`TYPECAST_VOICES`, `GOOGLE_VOICES`).
회사가 바뀌었다고 설정 화면이 둘에서 마흔하나로 늘어나면 안 됩니다.
`matchVoice()`가 **남녀를 지켜서 옮깁니다** — 안 그러면 「남자 목소리」로 해 둔 집이
어느 날 말없이 여자 목소리를 듣게 됩니다.

회사를 고른 자취:
- 클로바 → 월 고정비 9만 원. 우리 사용량으로는 그 돈의 1%도 못 씁니다.
- OpenAI → 목소리가 영어에 맞춰져 있어 한국어 받침이 흐려집니다.
- Google → 종량제라 좋았지만 **발음이 또렷하지 않았습니다.** 「송골」이 「쏭골」로 들렸습니다.
- 타입캐스트 → 한국어 전용. 들어 보고 골랐습니다. 대신 MP3가 **Google의 8배 크기**입니다.

회사마다 다른 것 — 새 회사를 붙일 때 여기부터 보세요:
- **쉼**: Google은 SSML `<break>`, 타입캐스트는 **그런 게 없어 쉼표를 끼워 넣습니다.**
- **글자 수**: Google은 SSML 태그까지 셉니다. 타입캐스트는 쉼표까지 셉니다.
- **목소리 목록**: Google은 언어로 걸러 주지만 **타입캐스트는 언어 정보를 아예 주지 않습니다.**
  1,125개 중 한국어를 가릴 방법이 없어 손으로 고른 목록을 씁니다(`TYPECAST_VOICES`).
- **기본 목소리**: 회사마다 형태가 달라(`ko-KR-…` vs `tc_…`) **서버가 정해서 알려 줍니다.**
  다른 회사의 이름이 기기에 남아 있어도 형태가 안 맞으면 조용히 기본값으로 돌아갑니다.

**폴백은 아이에게 조용히, 부모에게는 알립니다.** 아이 화면에 "음성 오류"가 뜨면 문제 풀다 멈춥니다.
대신 넘어간 사실을 기기에 적어 두고 보호자 설정에서 보여 줍니다(`TtsStatus`).
그러지 않으면 **며칠째 기기 목소리로 읽고 있어도 아무도 모릅니다.**

문장별로 브라우저 메모리에 소리를 담아 둡니다(`tts-app.ts`의 캐시, 80개).
**이 캐시가 "다시 듣기는 매번 같아야 한다"를 이미 보장합니다** — 한 세션은 화면 하나라
담아 둔 소리를 그대로 다시 틉니다. 생성형 목소리를 쓸 수 있는 이유가 이것입니다.

읽기 방식 3종: `flow`(이어 읽기) / `chunked`(어절마다 쉼) / `teacher`(전체→끊어서→전체)
**`chunked`는 낱말을 따로 합성하지 않습니다.** 그러면 TTS가 낱말마다 문장을 끝내듯 읽어
또박또박 들으려던 것이 오히려 더 안 들립니다. 한 번에 보내고 사이만 벌립니다.

**기본 속도는 1.0(보통)입니다.** 늦추면 소리를 늘려 재생해서 마찰음이 길어지고,
귀는 그걸 된소리로 듣습니다 — 「천천히」가 오히려 덜 또렷했습니다.
### 설정 — `src/lib/settings.ts`, `settings-server.ts`, `/api/settings`

**두 층입니다.**

```
부모가 정한 기본값(families.default_*)  →  아이가 고른 값(children.*)
```

아이 값이 비어 있으면 부모 기본값으로, 그것도 비면 앱 기본값으로 **항목마다 따로** 내려갑니다.
**비어 있음이 「안 골랐음」입니다.** 미리 채워 두면 부모가 나중에 기본값을 바꿔도 안 닿습니다.

| 무엇 | 어디서 고르나 | 어느 층 |
|---|---|---|
| 목소리 회사 | 보호자 설정 (키가 둘 다 꽂혔을 때만) | **family** |
| 목소리 | 보호자 설정 · **아이 설정** | family / child |
| 쓰기 방법 | 보호자 설정 · **아이 설정** | family / child |
| 읽는 속도 | 보호자 설정 · **받아쓰기 화면** | family / child |
| 효과음 | 양쪽 설정 | **기기**(localStorage) |

효과음만 기기에 둡니다 — 「지금 조용히 해야 하나」는 아이의 성향이 아니라 그 순간의 사정입니다.

**기기가 아니라 서버에 두는 이유**는 부모가 휴대폰, 아이가 패드를 쓰기 때문입니다.
예전에는 브라우저 한 곳에 통으로 저장해서 (1) 아이가 둘이면 한 값을 같이 썼고
(2) 부모가 정한 것이 아이 기기로 넘어가지 않았습니다.

화면은 **서버 컴포넌트가 읽어 props로 내려 줍니다**(`readSettings()`).
화면이 스스로 불러오면 기본값으로 한 번 그렸다가 바뀌는데,
그 사이에 아이가 「문장 듣기」를 눌렀으면 옛 속도로 읽어 버립니다.

**읽다가 잘못돼도 앱은 굴러갑니다** — 칸이 없거나 네트워크가 흔들리면 기본값으로 내려갑니다.

칸 이름을 대고 물었는데 **그 칸이 없으면 조회 전체가 실패합니다.**
새 칸 하나 때문에 속도·목소리·쓰기 방법까지 한꺼번에 못 읽어,
부모가 정해 둔 것이 전부 사라진 것처럼 보입니다.
그래서 `familyRow()` 는 실패하면 **예전 칸 목록으로 한 번 더 물어봅니다** —
배포와 마이그레이션의 순서에 앱이 매달리지 않게요.
저장은 기다리지 않습니다(`saveSettings`). 눌린 느낌이 먼저이고,
실패는 다음에 화면을 열 때 옛 값으로 드러나는 편이 매번 실패 문구를 보는 것보다 낫습니다.

어느 아이인지는 화면이 보낸 값이 아니라 **쿠키**(`bs_child`)로 정합니다 —
화면이 보낸 id를 믿으면 남의 집 아이 설정을 바꾸라고 보낼 수 있습니다.

목소리는 소리를 만드는 자리가 화면에서 멀어, 모듈 한 곳에 꽂아 둡니다(`setActiveVoice`).

**켜진 것은 상태가 아니라 계산입니다**(`VoiceSettings`).
props를 `useEffect`로 상태에 옮겨 담다가, 공급자가 바뀌어 저장된 이름(`tc_…`)이
지금 목록(`ko-KR-…`)에 없게 되자 **동그라미 두 개가 다 빈 화면**이 나왔습니다.
부모는 「골라 둔 게 사라졌나」 하고, 지금 뭘로 읽는지도 알 수 없습니다.

이제 매번 계산하고 **목록에 있는 것만** 켭니다 —
방금 누른 것 → 저장된 것 → 서버 기본값 → 목록 첫 줄.
마지막 갈래가 있어 목록이 비어 있지 않은 한 **언제나 하나는 켜집니다.**

거기에 더해 `readSettings()`가 저장된 목소리를 `matchVoice`로 지금 회사 이름으로 옮겨 둡니다.
재생과 목록이 **같은 함수**를 쓰므로 화면과 실제 소리가 갈리지 않습니다.
저장은 하지 않습니다 — 타입캐스트로 돌아가면 원래 고른 목소리가 살아나야 합니다.

**속도만 세션 화면에 둡니다.** 문장마다 「이건 잘 안 들려」가 있으니까요.
쓰기 방법을 세션에서 뺀 것은 취향이 아니라 **채점이 뒤집히기 때문**입니다.
원고지 모드에서만 쉼표 규칙을 문장으로 되돌리므로, 규칙대로 「마시고,빵도」라고 써 놓고
그냥 쓰기로 바꾸면 글자는 그대로인데 띄어쓰기를 틀렸다고 나옵니다.

### 짝 문제 — `src/lib/twin.ts`, `twin-ai.ts`, `twin-server.ts`

오답노트의 **두 번째 걸음**을 「같은 문장 한 번 더」에서 「같은 규칙, 다른 문제」로 바꿉니다.

**층이 둘이고, 값이 완전히 다릅니다.**

```
twin.ts        순수 로직. AI 도 네트워크도 없음
               · 맞춤법 짝 고르기 (문제은행 태그)      0원, 즉시  → 언제나 무료
               · 받아쓰기 후보 거르기                 0원        → 언제나 무료
──────────────────────────────────────────────────────────
twin-ai.ts     Claude 호출. AI 가 닿는 유일한 자리     몇 원      → 요금제 문(twinSentences)
```

**AI 층을 통째로 들어내도 맞춤법 짝은 그대로 돕니다.** 그게 분리가 제대로 됐다는 증거입니다.
그리고 무료에서도 오답노트가 그대로 돕니다 — 짝이 없으면 원본을 두 번 풀어 졸업합니다.
잠글 수 있는 것은 「AI 가 받아쓰기 문장을 만들어 주는 것」 하나이고,
그건 오늘 있는 것을 빼앗는 게 아니라 **얹는 것**입니다.

**맞춤법은 AI 가 필요 없습니다.** 오답을 저장할 때 문항의 `tag`를 `error_types`에
그대로 넣어 두므로(`SpellingRunner`), 같은 태그의 다른 문항을 고르면 끝입니다.
새로 저장할 데이터가 하나도 없습니다.

**AI 가 내고, 우리 규칙이 거릅니다**(`chooseTwin`). 후보를 셋 받아 통과한 첫 번째를 씁니다 —
하나만 받으면 떨어질 때마다 다시 불러야 하고 그동안 아이가 기다립니다.

| 검사 | 왜 | 기계가 보나 |
|---|---|---|
| 원고지 15칸 | 두 줄이 되면 확인 버튼이 밀립니다 | ✅ |
| **원본과 비슷한 길이** | 「포도」(2칸)의 짝으로 「책을 읽었어요」(8칸)가 나왔습니다 | ✅ |
| 원본과 다른 문장 | 같으면 애초에 고치려던 문제로 돌아갑니다 | ✅ |
| 겨눈 오류를 담는가 | 받침을 틀렸는데 받침 없는 문장이면 연습이 안 됩니다 | 여섯 중 넷 |
| **원본이 겹받침이면 짝도 겹받침** | 「닭」을 틀렸는데 「밥」을 내면 겨눈 자리를 비껴갑니다 | ✅ |
| 동음이의가 홀로 | 개/게는 귀로 못 가립니다 | ✅ |
| 한글만 | 영어·숫자는 받아쓰기가 안 됩니다 | ✅ |

**채점 엔진이 「이건 받침 오류다」를 판정할 때 쓰는 바로 그 분해기**(`hangul.ts`)로
「이 문장에 받침이 들어 있나」를 봅니다. 같은 자를 쓰니 어긋날 수가 없습니다.

**원본에 마침표가 없으면 짝에서도 뗍니다**(`matchPunctuation`).
「포도」의 짝으로 「복숭아.」가 나왔는데, 마침표는 **원본에서 안 물었던 것**입니다.

하나도 통과 못 하면 **예전 동작으로 조용히 내려갑니다** — 아이 화면에는 아무 일도 없습니다.

**미리 만들어 둡니다.** AI 는 2~5초 걸립니다. 아이가 원본을 맞힌 그 자리에서 부르면
그만큼 멈춰 있습니다. 그래서 **틀린 순간에**(`/api/sessions`, 기다리지 않음) 만들고,
오답노트를 열 때 놓친 것을 줍습니다(`/api/twin` ← `TwinFill`).
문장별로 소리를 미리 담아 두는 것과 같은 원리입니다.

**잘못된 짝이 뚫려도 잃는 것은 별 하나입니다.** 오답노트 풀이는 `logAttempt: false`라
리포트의 평균 점수에 안 들어갑니다. 사진으로 넣은 문장(부모가 꼭 확인해야 하는 것)보다
한 단계 낮은 위험이라, 부모 검토 단계를 두지 않았습니다.

**프롬프트를 고쳤으면 `twin-live.check.ts`를 돌려 보세요.**
실제로 Anthropic 을 부르는 확인용이라 `.check.ts`로 두어 평소 테스트에는 안 들어갑니다
(`.test.ts`로 이름을 바꿔 `npx vitest run`). 여기서 두 가지가 실제로 잡혔습니다 —
띄어쓰기 조건이 원본보다 엄격했던 것과, 겹받침을 비껴간 짝.

### 오답을 볼 틈 — `src/components/NextButton.tsx`
채점 결과에서 **틀렸을 때만** 「다음」을 2초 잠급니다. 남은 초를 버튼에 보여 줍니다.

아이는 「다음」 자리를 손가락으로 기억해서, 채점 화면이 뜨는 순간 이미 누르고 있습니다.
정답이 화면에 떠 있었는데도 못 보고 지나갑니다.
맞았을 때는 잠그지 않습니다 — 상 주는 자리가 벌 서는 자리가 되면 안 됩니다.
남은 초를 감추면 아이는 고장인 줄 알고 더 세게 누릅니다.

### 처음 오면 짚어 주기 — `src/lib/coach.ts`, `src/components/CoachMarks.tsx`

화면을 어둡게 덮되 **짚는 곳 하나만 뚫어서** 한 걸음씩 설명합니다.

예전에는 홈에 카드를 띄우고 다섯 걸음을 넘기게 했습니다(`Onboarding.tsx`, 없앴습니다).
문제는 **「사진으로 문제 넣기」처럼 다른 화면 안에 있는 것은 말로만 알려 줄 수밖에 없었다**는 점입니다.
말로 들은 것은 5분 뒤에 잊힙니다. 부모가 사는 것이 바로 그 기능인데요.

**그래서 화면마다 그 화면 것만 짚습니다.** 홈에서는 「사진으로 넣을 수 있어요」라고 예고만 하고,
부모가 실제로 세트 만들기에 들어왔을 때 거기서 앨범·촬영 버튼을 짚습니다.
**알려 주는 시점과 쓰는 시점이 같아집니다.**

| 화면 | 무엇을 짚나 |
|---|---|
| 홈(보호자) | 프로필 칩 → **문제 세트 만들기** → 리포트 |
| 홈(자녀) | 받아쓰기 → 오답노트 |
| 문제 세트 만들기 | **앨범·촬영 두 버튼을 함께** → 문장 목록(「글자를 꼭 봐 주세요」) |
| 받아쓰기 세션 | 문장 듣기 → 원고지 → 확인 |

짚을 곳은 `data-coach="…"` 로 표시합니다.
**CSS 선택자를 `coach.ts` 에 적어 두면 나중에 클래스 하나 바뀔 때 조용히 안 뜹니다** —
그리고 안 뜨는 안내는 아무도 신고하지 않습니다.

**화면에 없는 대상은 걸음에서 뺍니다**(`visibleSteps`).
아이 홈에는 리포트가 없고 세션의 「확인」은 쓰는 중일 때만 있습니다.
없는 것을 짚으면 빈 자리에 동그라미가 떠서 무엇을 보라는 건지 알 수 없습니다.

구멍은 SVG 마스크가 아니라 **빈 상자에 아주 넓은 `box-shadow`** 입니다. 가볍고 모서리도 그냥 둥글려집니다.

**덮개는 `pointer-events: none` 입니다 — 짚어 준 버튼이 실제로 눌러져야 합니다.**
설명만 읽고 끝나는 것과 그 자리에서 눌러 보는 것은 다릅니다.

| 어긋나기 쉬운 것 | 어떻게 |
|---|---|
| 대상이 화면 밖 | `scrollIntoView` 로 데려온 뒤, 멈출 때까지 600ms 동안 따라 잼 |
| 화면 회전·키보드 | `resize`·`scroll`(캡처)에서 다시 잼 |
| 화면 끝의 대상 | `balloonPlacement` 가 말풍선을 화면 안으로 밀어 넣음 |
| 아직 안 그려진 화면 | 350ms 기다렸다가 대상을 찾음(홈의 「오늘의 요약」이 늦게 옵니다) |
| 포커스 | 안내 전 자리를 기억했다가 **끝나면 돌려줌** — 아니면 아이가 키보드를 다시 불러야 합니다 |

**Enter 는 일부러 안 받습니다.** 세션 화면에서 Enter 는 「확인」이라,
안내를 넘기려다 답을 제출하게 됩니다. 넘기는 것은 → 키, 빠져나가는 것은 ESC 입니다.

세션 안내는 **첫 문장에서만**, 그것도 쓰기 단계에서만 뜹니다(`index === 0`).
보상이 아니라 안내라 절대 원칙 1에 걸리지 않지만, 푸는 흐름은 끊지 않아야 합니다.

기억은 **기기별**(localStorage)입니다. 부모 폰과 아이 태블릿이 따로라 각자 한 번씩 보는 게 맞습니다.
더보기의 「안내 다시 받기」가 네 화면 것을 한꺼번에 되살립니다(`resetCoach`).

**누른 자리에 그대로 머뭅니다.** 예전에는 표시를 지우고 홈으로 밀어냈는데
누른 자리와 도착한 자리가 달라 「내가 왜 홈에 와 있지」가 되고,
문제 만들기·세션 안내는 어차피 그 화면에 가야 뜨니 절반만 맞는 동작이었습니다.
이름도 바꿨습니다 — 「사용법 다시 보기」는 **지금 보여 주겠다**는 약속인데 이제 그렇지 않습니다.
화면이 안 바뀌므로 그 자리에서 「다시 켰어요 ✓」로 바뀝니다.
아무 반응이 없으면 고장인 줄 알고 몇 번씩 더 누릅니다.

### 홈 화면에 앉히기 — `src/app/manifest.ts`, `scripts/make-icons.cjs`

**스토어 앱은 만들지 않습니다.** 인앱결제 수수료가 15~30%라 학기권 19,000원에서 최대 5,700원이 나갑니다
(웹 결제는 3% 안팎). 여기에 심사 대기와 두 플랫폼 유지가 얹힙니다.
그리고 유통 경로가 맘카페·학부모 단톡방이라 **링크가 이깁니다** —
「앱 깔고 가입하세요」는 그 자리에서 절반이 떨어집니다.

대신 두 가지가 필요합니다.

1. **홈 화면 아이콘**(`manifest.ts` + `display: standalone`).
   아이가 매일 여는 물건이 주소창을 거쳐야 하면 결국 안 엽니다.
2. **카톡 공유 카드**(`layout.tsx` 의 `openGraph`).
   맨 주소만 뜨면 안 눌립니다.

**서비스워커는 일부러 두지 않습니다.** 문장을 읽어 주는 것이 서버라 오프라인에서는
받아쓰기가 성립하지 않고, 캐시가 한 겹 더 끼면 고친 화면이 언제 반영되는지 알기 어려워집니다.

아이콘은 **원고지 한 칸에 「받」**입니다. 홈 화면에서 본 것과 앱을 열었을 때 보는 것이 같아야 알아봅니다.

**아이콘은 빌드 때 그리지 않고 결과물을 저장소에 넣습니다.**
Vercel 에는 한글 글꼴이 없어 거기서 그리면 「받」이 네모로 나옵니다.
고칠 일이 있으면 `node scripts/make-icons.cjs` 로 다시 그려 커밋하세요(sharp 필요).

`metadataBase` 가 없으면 og:image 가 상대 주소로 나가 카톡이 못 읽습니다.

**브라우저는 설치를 거의 권하지 않습니다** — 그래서 우리가 직접 안내합니다(`src/lib/install.ts`).

| 어디서 보고 있나 | 무엇을 알려 주나 |
|---|---|
| `inapp` 카카오톡·네이버·인스타 | **먼저 브라우저로 나가는 법.** 거기엔 「홈 화면에 추가」가 아예 없습니다 |
| `ios` Safari | 공유 → 홈 화면에 추가 |
| `ios-other` 아이폰의 Chrome 등 | Safari 로 옮겨야 합니다. 다른 브라우저에는 그 메뉴가 없습니다 |
| `android` Chrome·삼성인터넷 | ⋮ → 앱 설치. 삼성은 자리가 달라 덧말로 |
| `desktop` | 아이가 쓸 폰·태블릿에서 열라고 넘김 |
| `standalone` | **아무것도 안 합니다.** 시킨 대로 한 사람에게 또 시키면 안 됩니다 |

**인앱 브라우저를 가장 먼저 판별합니다.** 카카오톡 UA 에도 Chrome·Safari 가 들어 있어서,
뒤에서 보면 평범한 Chrome 으로 잘못 읽고 **있지도 않은 메뉴를 누르라고** 합니다.
단톡방이 유통 경로라 거의 모든 첫 방문이 여기를 지납니다 — 여기가 제일 많이 막히는 자리입니다.

iOS 는 설치 안내가 **아예 없고**(애플이 지원하지 않습니다),
안드로이드 Chrome 의 자동 배너는 **서비스워커를 요구합니다**(우리는 두지 않았습니다).
`beforeinstallprompt` 가 오면 버튼 하나로 끝내지만, 대개 안 옵니다 — 안 와도 안내는 그대로 뜹니다.

권하는 자리는 둘입니다. **더보기에 한 줄**(늘), **홈에 카드 한 번**(세 번째 방문부터, 보호자 화면만).
첫 방문에 들이밀면 아직 뭔지도 모르는 물건을 홈 화면에 두라는 말이 됩니다.
아이 화면에는 안 띄웁니다 — 아이가 할 수 있는 일이 아닙니다.

**`manifest.webmanifest` 를 프록시에서 빼는 것을 잊지 마세요.**
확장자가 png·svg 가 아니라 기존 규칙에 안 걸리는데, 로그인으로 막히면
아직 로그인하지 않은 기기에서 「홈 화면에 추가」가 이름도 아이콘도 없이 붙습니다.

### 효과음 — `src/lib/sfx.ts`
음원 파일 없이 Web Audio로 생성합니다. 짧고 부드럽게 유지하세요.
오답은 벌 주는 소리가 아니라 주의를 옮기는 신호 정도여야 합니다.

### 디자인 시스템 — `src/app/globals.css`
소재는 원고지(국어 공책의 격자), 빨간펜 교정, 연필 흑연입니다.

**색마다 역할이 하나씩입니다.**
- 초록(`--grid`) = 구조와 정답
- 빨강(`--pen`) = 교정
- 금색(`--gold`) = 보상. **보상 화면과 별 표시 외에는 쓰지 마세요.**
  금색이 흔해지면 보상의 의미가 사라집니다.

표제는 명조(`Gowun Batang`, `.display` 클래스), 본문은 Pretendard입니다.
`.grid-cell` 계열이 원고지 칸이고, 이게 이 앱의 시그니처입니다.

---

### 리포트 — `src/lib/report.ts`, `src/app/report/page.tsx`

**견줄 수 없는 것을 견주지 않습니다.**

예전에는 세션 점수를 **전부 한 평균**에 넣었습니다. 그러면 받아쓰기 20단계 시험과
맞춤법 1단계 연습이 같은 숫자에 들어갑니다. 「지난주 75 → 이번 주 82」가
**실력이 는 것인지 쉬운 걸 골라 푼 것인지 구분할 방법이 없었습니다** —
리포트에서 가장 크게 뜨는 숫자가 가장 못 믿을 숫자였습니다.

지금은 **과목 × 방식**으로 갈라 셉니다(`buildTracks`). 한 줄 안에서만 견줍니다.
갈래가 넷이지만 **기록이 있는 것만** 나가므로 실제로는 한둘입니다.
시험을 앞에 둡니다 — 연습은 몇 번이든 다시 할 수 있어서
「지금 얼마나 아는가」는 시험 쪽이 더 정직합니다.

**줄을 가로질러 견주면 안 되므로 막대가 아니라 숫자로 둡니다.** 막대는 서로 견주라는 뜻입니다.
평균 옆에 **몇 번 풀어서 나온 값인지**도 적습니다 — 한 번짜리 평균과 다섯 번짜리는 무게가 다릅니다.

**기록이 없는 주는 0점이 아니라 「—」입니다.** 안 한 것과 못 한 것은 부모에게 완전히 다른 이야기입니다.

**단계 난이도는 아직 못 가립니다.** 화면에 그렇게 적어 두었습니다.
lv3 과 lv12 를 같은 줄에서 견주는 것은 여전히 남은 문제입니다.

**성실성은 「7일 중 3일」로 씁니다**(`daysIntoWeek`).
그냥 「3일」이면 수요일의 3일과 일요일의 3일이 같아 보입니다 —
앞의 것은 하루도 안 빠진 것이고 뒤의 것은 나흘을 쉰 것인데요.

**오답 유형 막대는 「몇 번 틀렸나」가 아닙니다.**
그 유형이 든 **오답노트가 몇 개인가**입니다. 같은 문장을 열 번 틀려도 1이고,
졸업한 노트도 그대로 셉니다. 셈을 바꾸려면 오답 이력을 따로 쌓아야 하므로
지금은 **화면에 뜻을 그대로 적어 두었습니다.**

**오답 이력을 쌓기 시작했습니다** — `wrong_events`, `src/lib/wrong-events.ts`.
아직 **화면에는 안 씁니다.** 과거 이력이 없어 지금 붙이면 빈 화면만 보입니다.
두어 주 쌓인 뒤에 붙이는 편이, 무엇을 보여 줄지도 그때 더 선명해집니다.

**틀린 것만 쌓습니다.** 받아쓰기에서 오류 유형은 틀렸을 때만 생기고,
분모(푼 문제 수)는 이미 `attempts.total_count` 에 있습니다.
맞힌 것까지 저장할 까닭이 없습니다 — 아이 정보는 적을수록 좋습니다(절대 원칙 7).

| 칸 | 이게 없으면 |
|---|---|
| `created_at` | **나아짐을 못 봅니다.** 이 표의 존재 이유입니다 |
| `attempt_id` | 최근 기록을 눌러도 그날 뭘 틀렸는지 못 엽니다. `builtin_set_id` 로 조인하면 단계별도 |
| `source` | 복습이 시험 통계에 섞입니다. 분모에 복습이 없어 오답률이 부풀어요 |
| `input` | 「받침 8건」까지만 알고 **어떻게** 틀리는지 모릅니다 |
| `content` | 부모가 세트를 고치면 옛 사건이 미아가 됩니다(`ref_id` 가 문장 원문이라) |

**쌓인 줄은 안 바뀌고 안 지워집니다.** 부모가 오답노트를 지워도 이력은 남습니다 —
노트는 「할 일」이고 이력은 「일어난 일」이라 다른 것입니다.
목록을 정리했다고 과거가 사라지면 **리포트가 정리 습관에 따라 달라집니다.**
대신 지우기 확인 문구에 「리포트 기록은 남아요」를 적어 기대와 어긋나지 않게 했습니다.

### 세션 저장 — `src/app/api/sessions/route.ts`
**기록·오답노트·보상이 한 라우트에서 한 번에 일어납니다.**
세션을 끝까지 마쳤을 때만 부릅니다(절대 원칙 2). 중간에 나가면 아무것도 보내지 않습니다.

- 점수는 화면이 보낸 값을 쓰지 않고 서버가 `scoreOf()`로 다시 계산합니다.
- `logAttempt: false`는 오답노트에서 한 문제만 그 자리에서 풀 때만 씁니다.
  한 문제짜리 0점/100점이 쌓이면 리포트의 평균 점수가 흔들리기 때문입니다.
  이때도 별·졸업은 그대로 반영됩니다.
- 날짜는 언제나 `toDateKeyInSeoul()`·`seoulWeekStart()`로 셉니다.
  서버가 UTC로 돌면(Vercel) 아침 기록이 어제로 밀려 리포트의 "오늘"과 "이번 주"가 어긋납니다.
- 보상은 `trophyFor(score, mode)`가 정합니다. 연습 모드는 언제나 null입니다.

화면 쪽은 `saveSession()`(`src/lib/save-session.ts`)으로 부르고,
**실패해도 결과를 손에 쥔 채 다시 보낼 수 있게** 해 두었습니다.
끝까지 푼 아이에게 "처음부터 다시"라고 말하지 않기 위해서입니다.


### 남에게 열기 전에 — `src/lib/plan.ts`, `error.tsx`, `/api/log`

**요금제는 자리만 만들어 두고 아직 켜지 않았습니다**(`PLAN_ENABLED = false`).
결제가 없는 상태에서 한도만 걸면 돈을 낼 길도 없이 막히는 사람이 생깁니다.
켜는 것은 그 값 하나를 바꾸는 일이 되도록, 막을 자리를 미리 잡아 두었습니다
(`/api/ocr`, `/api/children`).

**원가로 가르지 않습니다.** 재어 보니 원가가 거의 안 듭니다 —
문장 평균 9.2자, 한 세션 10문장이면 이어 읽기 92자, 한 가족이 월 2,000~8,000자입니다.
다시 듣기는 브라우저 캐시라 공짜이고 Google 무료 한도가 월 100만 자입니다.
「많이 쓰면 손해」인 구조가 아니라 고정비가 거의 전부입니다.

| | 무료 | 유료 |
|---|---|---|
| 하루 세션 | 2 | 무제한 |
| 자녀 프로필 | 1 | 4 |
| 사진으로 문제 넣기 | ✕ | ○ |
| 리포트 | 이번 주 | 전부 |

**원고지·채점·오답노트는 한도 목록에 아예 없습니다.** 없다는 것이 곧 「잠글 수 없다」는 뜻입니다.
원고지는 제품의 정체성이고 원가도 0원이라 잠글 이유가 하나도 없습니다.
테스트가 그 목록을 지킵니다 — 누가 거기에 원고지를 더하려 하면 걸립니다.
**소리도 전면 차단하지 않습니다.** 막으면 받아쓰기가 성립하지 않습니다.

**화면이 깨지면 서버에 알립니다**(`error.tsx` → `/api/log` → Vercel 로그).
예전에는 남의 집에서 깨져도 아무도 몰랐습니다 — 브라우저 콘솔에만 남고 그 콘솔은 아무도 안 봅니다.
완전한 추적 도구는 아니지만 계정도 결제도 없이 오늘 되는 것이고, 없는 것보다 낫습니다.
나중에 Sentry 같은 것을 붙이면 `/api/log` 한 곳만 갈아 끼우면 됩니다.
`global-error.tsx`는 레이아웃까지 깨졌을 때의 그물이라 `globals.css`를 못 씁니다 — 색을 직접 적습니다.

**고지 문서는 `src/lib/legal.ts` 한 곳을 봅니다.** 운영자와 연락처를 두 문서에 따로 적어 두면
연락처를 바꿀 때 한 곳만 고쳐지고, 부모가 안 쓰는 주소로 문의를 보내게 됩니다.
문의처는 더보기 맨 아래에도 둡니다 — 방침 문서를 끝까지 내려 볼 사람은 없습니다.

**아직 없는 것** — 사업자 정보 표기, 환불 규정, 결제. 돈을 받기 전에 갖춰야 합니다.
그리고 Vercel Hobby 요금제는 **상업적 사용이 금지**입니다. 돈을 받으면 Pro여야 합니다.

### 화면 구성 요약

| 경로 | 누가 | 하는 일 |
|---|---|---|
| `/dictation` → `/dictation/[id]` → `/[id]/play?mode=` | 자녀 | 세트 고르기 → 연습/시험 → 세션 |
| `/dictation/new`, `/dictation/[id]/edit` | 보호자 | 세트 만들기·고치기 (사진 인식 포함) |
| `/spelling` → `/spelling/[kind]?mode=` | 자녀 | 문제은행에서 10문제, 서버에서 섞음 |
| `/notes` | 자녀 | 오답노트. **항목을 그 자리에서 펼쳐 풉니다** (`InlineNotePractice`) |
| `/notes/practice?module=` | 자녀 | 쌓인 오답을 몰아서 풀기. 언제나 연습 모드(별만, 배지 없음) |
| `/report?child=` | 보호자 | 주차별 평균, 오답 유형, 최근 기록 |
| `/trophies` | 둘 다 | 보관함 |
| `/more` | 둘 다 | 묶음 메뉴 |
| `/settings` | 둘 다 | 부모는 **기본값**(속도·목소리·쓰기 방법)과 집 관리, 아이는 **자기 것**(효과음·목소리·쓰기 방법) |
| `/auth/confirmed` | — | 가입 메일 링크가 닿는 자리. **아무것도 하지 않습니다** |
| `/terms` | — | 이용약관 |
| `/privacy` | — | 개인정보처리방침 |

`/auth/confirmed`가 비어 있는 것은 의도입니다.
메일은 대개 **다른 기기**에서 열리는데, 거기서 로그인 세션까지 만들려면
가입할 때 그 브라우저에 넣어 둔 열쇠가 필요합니다. 폰에는 없으니 실패합니다.
확인은 링크를 누르는 순간 Supabase에서 끝나므로, 우리는 "끝났어요"만 보여 주면 됩니다.

## 내장 문제은행 — `src/data/dictation-bank.ts`, `src/data/spelling-bank.ts`

**받아쓰기**는 학년·학기 표시가 없습니다. **1단계부터 20단계**로만 올라갑니다.
「급」이 아니라 「단계」인 이유: **학교 문제지가 「급」을 씁니다.**
사진으로 넣은 학교 문제와 내장 문제가 목록에 나란히 놓이는데 둘 다 「3급」이면 아이가 헷갈립니다.
목록에서도 **직접 넣은 문제가 위**입니다 — 학교에서 받아온 것이 오늘 할 일이니까요.

실제 학교 받아쓰기 자료(1학년 2학기용)를 열어 난이도 진행 순서만 참고했고,
낱말·문장은 전부 새로 썼습니다(저작권 있는 원문을 그대로 옮기지 않습니다).
id는 `lv1`~`lv20` 형태이고 `attempts.builtin_set_id`에 그대로 남습니다 —
**한 번 배포한 id는 바꾸지 마세요.** 바꾸면 아이가 쌓은 점수와 이어지지 않습니다.

**문장을 고치는 것도 조심하세요.** 받아쓰기 오답노트는 `refId`로 **문장 원문 자체**를 씁니다.
문장을 바꾸면 그 문장에 걸린 오답노트가 끊깁니다.
반면 맞춤법은 문항 id를 쓰므로 문제·보기·설명을 고쳐도 이어집니다.
**단계 이름과 `focus`는 표시용이라 언제 바꿔도 안전합니다.**

**20단계를 뺀 모든 문장은 원고지 한 줄(15칸)에 들어갑니다.** 테스트가 지킵니다.
20단계만 예외입니다 — 거기는 두 문장을 이어 받아쓰는 것이 목적이라
칸에 맞추려고 줄이면 마지막 단계가 마지막 단계가 아니게 됩니다.

받아쓰기 문장에는 ㅐ/ㅔ가 모두 실제 낱말인 경우(개/게, 새/세, 배/베)를 홀로 쓰지 않습니다.
소리만으로는 정답을 정할 수 없기 때문입니다. 대신 그런 낱말은 **맞춤법** 쪽에서
문맥(문장)과 함께 다룹니다 — `개/게`, `새/세`, `배/베` 태그로 들어 있습니다.

**맞춤법**은 76문항(mcq 47 / fill 11 / find 18)이고 `level`(basic/inter/advanced)이 붙어 있습니다.
`pickBalanced()`가 세션마다 기본·중급을 많이, 고급을 조금만 섞어서 냅니다.
완전 무작위(`pickQuestions()`)로 뽑으면 저학년이 고급 문항을 몰아 받아
시험 점수가 실력과 무관하게 갈릴 수 있어서, 세션 화면은 반드시 `pickBalanced()`를 씁니다.
level은 지금 데이터에만 있고 화면에서 난이도를 고르는 기능은 아직 없습니다.

## 지금 어디까지 왔나

**배포되어 실제로 쓰고 있습니다.** 화면·콘텐츠·개인정보처리방침·카카오 로그인 다 끝났습니다.

| | |
|---|---|
| 주소 | badasseugi-wine.vercel.app |
| 앱 서버 | Vercel 서울(`icn1`) |
| 데이터베이스 | Supabase 도쿄(`ap-northeast-1`) — **리전은 나중에 못 바꿉니다** |
| 음성 | 타입캐스트 (키를 지우면 Google로) |

### 손대면 좋을 것 (급하지 않음)

- **회원가입 오류 메시지가 원인을 안 가립니다.** 지금은 무엇이 틀렸든 한 문구만 뜹니다.
- **안 쓰는 `spelling_bank` 테이블.** 맞춤법 문제는 코드에 있어서 이 테이블은 비어 있습니다.
- **화면당 데이터 조회 횟수.** 리전을 서울로 옮겨 많이 나아졌으니 재 보고 판단하세요.
- **원고지 입력 2단계.** 지금은 자동완성이 그대로 뜹니다.
  글자를 즉시 거둬들여 입력칸을 비우면 막히는데, 키보드 앱마다 동작이 달라 실기기 확인이 필요합니다.

### 환경 변수

`.env.example`이 단일 출처입니다. 여기서는 **판단이 달린 것만** 적습니다.

| 변수 | 없으면 |
|---|---|
| `TYPECAST_API_KEY` | Google로 읽습니다. **지우는 것이 곧 되돌리기입니다** |
| `GOOGLE_TTS_API_KEY` | 둘 다 없으면 브라우저 내장 음성 |
| `TTS_DAILY_LIMIT` | 타입캐스트 500 / Google 20,000 (회사마다 기본이 다릅니다) |
| `TYPECAST_VOICE_ID` | `TYPECAST_VOICES`의 첫 번째 |

**서비스 롤 키는 일부러 두지 않습니다.** 넣으면 RLS를 통째로 우회해 가족별 분리가 무너집니다.

타입캐스트 무료는 **월 15,000자**입니다. 한 세션이 200자쯤이라 하루 500자로 잡아 두었습니다.
**타입캐스트일 때는 `TTS_DAILY_LIMIT`으로도 500자 위로 못 올립니다**(`TYPECAST_MAX_DAILY`).
한 변수를 두 회사가 나눠 쓰는데 감당하는 양이 40배 달라서, Google 기준(20,000)을 넣어 둔 채
타입캐스트로 바꾸면 월 한도를 하루에 넘겨 씁니다. **실제로 그렇게 막혔습니다** —
합성이 403 `UNUSUAL_ACTIVITY_DETECTED`를 받았고 목소리 목록(200)은 멀쩡했습니다.
막힌 것은 `tts-blocked`로 갈라서 보호자에게 "기다려도 안 풀린다"고 알립니다.
Google은 월 100만 자까지 무료라 훨씬 넉넉합니다.

### 알아 두면 헤매지 않을 것

- **시간대는 언제나 한국으로 못 박습니다.** Vercel 은 UTC 로 돌고 이 PC 는 서울이라,
  **여기서는 멀쩡하고 배포하면 하루가 어긋납니다.** 실제로 세 곳이 그랬습니다 —
  트로피·최근 기록 날짜(전날로 찍힘), TTS·OCR 하루 한도(아침 9시에 리셋),
  세트 이름 제안(「8월 31일 받아쓰기」).
  화면에 날짜를 그리는 것은 대개 **서버 컴포넌트**라 Vercel 에서 돕니다 —
  `toLocaleDateString` 에 시간대를 안 주면 그 자리의 시간대를 씁니다.
  화면용은 `formatSeoulDate()`, 날짜 키는 `toDateKeyInSeoul()` 을 쓰세요.
  **테스트도 UTC 로 돌립니다**(`vitest.config.ts`) — 서울에서 돌리면 이 버그가 안 잡힙니다.
- **파이썬과 LibreOffice가 이 PC에 없습니다.** 파이썬은 스토어 별칭뿐이라 조용히 아무것도 안 합니다.
  문서·엑셀을 만들 일이 있으면 Node(`exceljs`, `docx`)를 쓰세요.
- **`perl -0777 -pe`로 여러 줄을 치환하면 파일이 깨진 적이 여러 번 있습니다.**
  소스 편집은 Edit 도구나 Node 스크립트를 쓰세요.
- **개발 서버가 켜져 있을 때 `npm run build`를 돌리면 서로 `.next`를 물어 죽습니다.**
- **미리보기 패널에서 화면을 확인할 때는 `loading.tsx` 두 개를 잠깐 치워야 합니다.**
  (`src/app/loading.tsx`, `src/app/report/loading.tsx`)
  두면 서버가 그린 HTML 은 보이는데 **하이드레이션이 끝나지 않습니다** —
  `useEffect` 가 아예 안 돌아, 코치마크·설치 안내처럼 화면에서 판단하는 것이
  전부 조용히 안 뜹니다. **코드 문제로 착각하기 딱 좋습니다**(실제로 두 번 그랬습니다).
  콘솔에는 아무 오류도 안 남습니다. 확인이 끝나면 **반드시 되돌려 놓으세요.**

---

## 기술 스택

| 영역 | 선택 |
|---|---|
| 프레임워크 | Next.js 15 (App Router) + TypeScript strict |
| 스타일 | Tailwind + CSS 변수 (`globals.css`가 단일 출처) |
| 백엔드 | Supabase (Postgres, Auth, Storage) |
| 인증 | 이메일 + 카카오 |
| 테스트 | Vitest |
| 배포 | Vercel |

---

## 개발 명령

```bash
npm install
npm run dev          # 개발 서버
npm test             # 테스트 (채점 로직 수정 시 필수)
npm run test:watch   # 테스트 감시 모드
npx tsc --noEmit     # 타입 검사
```

## 설정 순서

1. `.env.example`을 `.env.local`로 복사하고 값을 채웁니다
2. Supabase 프로젝트를 만들고 `supabase/migrations/`의 파일을 **번호 순서대로 모두** 실행합니다
   (`0001_init` → `0002_parent_pin` → `0003_tts_usage` → `0004_builtin_sets` → `0005_drop_spelling_bank` → `0006_settings` → `0007_engine` → `0008_twin` → `0009_wrong_events`).
   이미 돌아가고 있는 프로젝트라면 아직 안 돌린 번호만 실행하면 됩니다.
3. Supabase 대시보드 > Authentication 설정
   - **URL Configuration > Redirect URLs**에 아래를 등록합니다. 여기에 없는 주소로는 되돌아오지 못합니다.
     `http://localhost:3000/auth/callback`, `https://<배포주소>/auth/callback`
   - **Providers > Kakao**를 켜고 카카오 개발자센터의 REST API 키와 Client Secret을 넣습니다.
     카카오 쪽 Redirect URI에는 Supabase가 알려주는 `https://<project>.supabase.co/auth/v1/callback`을 넣습니다
     (우리 앱 주소가 아닙니다).
   - 이메일 확인을 켜 두면 가입 직후 세션이 없습니다. 화면은 "메일함을 확인해 주세요"로 안내합니다.
4. `npm run dev`

---

## 코드 작성 시 지켜야 할 것

- **한국어 주석을 답니다.** 이 프로젝트는 한국어 학습 도메인이라 용어를 한국어로 유지하는 게 명확합니다.
  다만 "무엇을 하는지"가 아니라 **"왜 그렇게 했는지"**를 씁니다.
- **UI 문구는 아이가 읽을 수 있게.** 어려운 한자어를 피하고, 동작 이름은 화면 전체에서 통일합니다.
  ("제출하기" 버튼을 눌렀으면 결과에도 "제출"이라는 말을 씁니다.)
- **실패 화면과 빈 화면은 다음 행동을 제안합니다.** 상태만 알리고 끝내지 않습니다.
- **접근성 기본선**: 키보드 포커스가 보이게, 모바일까지 반응형, `prefers-reduced-motion` 존중.
  이건 이미 `globals.css`에 들어가 있으니 깨뜨리지만 마세요.
