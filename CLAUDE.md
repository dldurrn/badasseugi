# 받아쓰기 공책 — 개발 지침

초등 저학년 자녀를 위한 받아쓰기·맞춤법 학습 웹앱입니다.
부모가 문제를 넣어 주고, 아이가 혼자 듣고 풀고 채점받고, 부모는 리포트로 확인합니다.

---

## 절대 원칙 — 임의로 바꾸지 마세요

이 항목들은 기획 단계에서 여러 차례 논의해 확정한 것입니다.
구현 편의를 위해 바꾸면 제품의 의도가 무너집니다.

1. **보상은 최종 결과에만.** 문제를 푸는 도중에는 축하 연출을 넣지 않습니다.
   시험 모드를 끝까지 마쳤을 때만 카드(100점)·배지(90점 이상)를 줍니다.
   연습 모드에는 보상이 없습니다.
2. **끝까지 마쳐야 기록에 반영됩니다.** 중도 이탈 시 점수·오답노트·보상 모두 저장하지 않습니다.
   나가기 전 확인 창을 띄웁니다.
3. **제출 전 최종 확인 단계.** "확인"을 누르면 바로 채점하지 않고,
   입력값을 보여주고 "다시 고치기 / 제출하기"를 한 번 더 거칩니다.
4. **입력창의 브라우저 맞춤법 교정을 끕니다.**
   `spellCheck={false}` `autoCorrect="off"` `autoCapitalize="off"`
   빨간 물결 밑줄이 뜨면 정답을 미리 알려주는 셈입니다.
5. **오답노트 졸업은 "연속 2회". 날짜는 따지지 않습니다.**
   두 번 잇달아 맞히면 졸업입니다. 오늘 안에 끝낼 수 있어야
   오답노트가 밀린 숙제가 아니라 치울 수 있는 목록이 됩니다.
   두 번을 요구하는 이유는 시간 간격이 아니라 **찍어서 맞힌 것을 거르기 위해서**입니다.
   (2지선다를 연속 두 번 맞힐 확률은 25%)
   한 번이라도 틀리면 별은 0으로 돌아갑니다.

   > 예전에는 "서로 다른 날짜"를 요구했습니다. 같은 날 반복은 단기기억이라는 이유였는데,
   > 아이가 오늘 아무리 해도 진도가 안 나가는 쪽의 손해가 더 컸습니다. 이 전제는 버렸습니다.
6. **채점 결과는 위아래 정렬 비교로.** 내가 쓴 글자와 정답 글자를 같은 세로줄에 놓습니다.
   두 줄을 따로 그리면 길이가 달라 어긋나서 비교가 안 됩니다.
   띄어쓰기 오류는 ∨(띄어요) / ✕(붙여요) 표식으로 위치를 짚어 줍니다.
7. **아동 개인정보 최소화.** 자녀 실명을 받지 않습니다. 별명만 씁니다.
   계정은 부모 이메일로만 만듭니다. (만 14세 미만 아동 정보 처리 시 법정대리인 동의 요건)
8. **API 키는 절대 클라이언트에 노출하지 않습니다.**
   OCR·TTS 등 외부 API는 반드시 서버 라우트를 경유합니다.
9. **부모 전용 화면(문제 세트 만들기, 리포트)은 자녀 화면에서 막습니다.**
   메뉴에서 감추는 데 그치지 않고, 주소를 직접 쳐도 화면이 열리지 않습니다 —
   「보호자 화면에서 볼 수 있어요」와 「프로필 바꾸기」 버튼이 대신 나옵니다.
   막으려는 것은 리포트 엿보기가 아니라 **정답 문장 열람**입니다 —
   보호자 화면에서 세트를 열면 정답이 그대로 보이니까요.

   **PIN은 자녀 프로필에 들어갈 때 묻습니다**(선택 사항, 안 걸면 그냥 들어갑니다).
   형이 동생 기록에 들어가는 것을 막는 용도입니다.
   **보호자 화면으로 넘어갈 때는 묻지 않습니다** — 화면을 감추는 정도로만 나눕니다.
   그 밖의 엄격한 서버 권한 검증은 범위 밖입니다.

   > 예전에는 이 항목이 "보호자 화면으로 넘어갈 때 PIN을 묻는다"고 적혀 있었는데,
   > 아래 인증 절에는 "보호자 화면 전환에는 잠금이 없습니다"라고 적혀 있었습니다.
   > 실제 동작은 후자입니다. 실제에 맞춰 고쳤습니다.
10. **쓰기 편한 쪽을 고릅니다. 코드가 조금 복잡해지는 것은 감수합니다.**
    구현이 단순하다는 이유로 손이 더 가는 화면을 만들지 않습니다.
    걸음 하나를 줄이거나 화면 왕복 하나를 없앨 수 있다면 그쪽을 택합니다.

    다만 **품이 크게 들거나 나중에 고치기 어려워질 것 같으면 먼저 알립니다.**
    무엇을 얻고 무엇을 잃는지, 더 싼 대안이 있는지 함께 적어 판단을 넘깁니다.
    말없이 어려운 길로 들어가지도, 말없이 쉬운 쪽으로 타협하지도 않습니다.

---

## 이미 구현되어 있는 것 (건드릴 때 주의)

### 채점 엔진 — `src/lib/grading.ts`
**전체 265개 테스트가 통과한 상태입니다. 수정하면 반드시 `npm test`로 확인하세요.**

- 편집거리(Levenshtein) 정렬로 정답과 입력을 글자 단위로 맞춥니다.
- 역추적 시 대체(sub)를 우선해서 같은 자리 글자끼리 짝지어집니다.
  이게 깨지면 "받침만 다름"을 감지하지 못합니다.
- 오류 유형 6가지: `batchim` `vowel` `consonant` `spacing` `punct` `letter`
- `columns` 배열이 화면 렌더링의 단위입니다.
  각 열은 위(내가 쓴 것)/아래(정답) 한 쌍이거나, 띄어쓰기 표식입니다.
- **같은 글자가 이어지면 표시를 뒤쪽으로 밉니다**(`shiftGapsRight`).
  「접다..」에서 두 점 중 어느 것이 군더더기인지 편집거리는 가리지 못합니다.
  앞엣것을 짚으면 바르게 찍은 점이 틀렸다고 나오고 덧붙인 점은 맞았다고 나옵니다.
  DP와 역추적은 건드리지 않고, 정렬이 끝난 뒤 `columns`만 다듬습니다.

검증 방식:
- `grading.test.ts` — 48개 구체 케이스 (한글 11,172자 전체 왕복 검증 포함)
- `grading.fuzz.test.ts` — 무작위 2,500회 속성 기반 교차 검증
- `wongoji.test.ts` — 원고지 칸 규칙·왕복·칸 이동 81개
- `review.test.ts` — 별·졸업 규칙 20개
- `spelling-bank.test.ts` — 문제은행 무결성·난이도 분포 19개
- `dictation-bank.test.ts` — 단계 구조·문장 품질 14개
- `pin.test.ts` — PIN 해시·프로필 전환 판정 12개
- `sets.test.ts` — 세트 입력 정규화, 한국 시간 날짜·주 계산 14개
- `auth-messages.test.ts` — 가입·로그인 오류를 원인별로 다르게 안내하는지 15개
- `grade-cells.test.ts` — 원고지 채점표의 칸 수가 쓰던 원고지와 같은지 22개
- `tts-engines.test.ts` — 공급자 넘어가기·하루 한도 상한·목소리 옮기기 17개

### 원고지 — `src/lib/wongoji.ts`, `src/components/Wongoji*.tsx`
아이가 학교 공책처럼 **칸마다 한 글자씩** 씁니다(한 줄 15칸).
**보호자 설정**에서 「그냥 쓰기」로 바꿀 수 있습니다 — 세션 화면에는 두지 않습니다(아래 음성 절).

**원고지는 보이는 방식일 뿐이고, 저장·채점은 언제나 원래 문장으로 합니다.**
쉼표 때문입니다 — 원고지에서 쉼표 뒤는 한 칸을 비우지 않는데 문장에는 공백이 있습니다.
격자를 그대로 이어 붙이면 "호로록,한 입"이 되어 **바르게 쓴 아이가 틀렸다는 채점을 받습니다.**

| 무엇 | 언제 |
|---|---|
| `toCells` | 정답·미리보기 — 원고지 규칙 적용(쉼표 뒤 공백 흡수) |
| `toWritingCells` | **아이가 쓰는 중** — 다듬지 않고 누른 대로 |
| `toText` | 칸 → 문장. 쉼표·마침표 뒤 공백을 되살립니다 |
| `writingToText` | **제출 직전 반드시 거칩니다.** 「그냥 쓰기」에서는 부르면 안 됩니다 |

**쓰기·확인·채점 세 화면이 모두 원고지입니다.** 가운데(확인 단계)만 한 줄짜리 글이었는데,
하필 거기가 「내가 쓴 게 이거 맞나?」를 묻는 자리라 견줄 것을 다른 모양으로 내밀고 있었습니다.
쉼표를 규칙대로 붙여 쓴 아이가 공백이 되살아난 문장을 보고 「다시 고치기」를 눌렀습니다.

격자가 담긴 상자를 꽉 채워야 하는 자리에는 `fill` prop을 줍니다(`.wg--fill`).
안 주면 528px에서 멈춰 라벨·버튼과 세로선이 어긋납니다 — 기둥이 둘로 보입니다.

`toCells`를 쓰는 화면에 그대로 쓰면 끝의 공백이 잘려 **스페이스가 안 먹는 것처럼 보입니다.**

입력은 격자 위에 투명하게 덮은 **입력칸 하나**가 받습니다. 칸마다 입력칸을 두면
`ㅇ → 여 → 예` 조합이 칸 경계에서 끊기고, 그 동작이 키보드 앱마다 다릅니다.

**그 입력칸은 속으로 한 줄짜리라, 「줄」이 필요한 동작은 전부 손으로 되짚어야 합니다.**

| 무엇 | 그냥 두면 | 그래서 |
|---|---|---|
| 누른 자리 | 둘째 줄을 눌러도 첫째 줄로 | `cellToTextOffset`으로 커서를 다시 잡음 |
| ↑ ↓ | 맨 앞·맨 끝으로 튐 | `cellByRow`로 15칸씩 |
| Home / End | 글 전체의 처음·끝 | `rowStart`·`rowEnd`로 그 줄 안에서만 |
| 지우개 | 언제나 **앞 칸**을 지움 | `eraseCell`로 **짚은 칸**을 지움 |

지우개가 제일 어긋났습니다. 종이 원고지에서는 칸을 짚고 지우면 그 칸이 지워지는데,
입력칸의 백스페이스는 앞 칸을 지웁니다 — 「학꾜에」의 「꾜」를 고치려고 짚고 지우면 「학」이 사라집니다.
**짚은 칸에 글자가 있을 때만** 바꿔치기하고, 끝에서 이어 쓰는 중이면 원래 동작에 맡깁니다.

**조합 중(`isComposing`)에는 어떤 키도 가로채지 마세요.**
조합 중 백스페이스는 「각 → 가 → ㄱ」처럼 자모를 하나씩 떼는 한글 고유 동작입니다.

커서 칸은 **포커스가 있을 때만** 진하게 칠합니다(`wg-cell--here`) — 없으면 옅게(`wg-cell--cursor`).
언제나 같은 세기로 칠했더니 지금 쓰는 중인지 그냥 표시인지 구분이 안 됐습니다.

**넓히는 것은 격자가 아니라 화면 전체입니다**(`.page--write`, 안쪽 720px).
오답노트에서 그 자리 풀기도 같습니다 — `.note--writing`이 **카드째** 넓힙니다.
좁은 카드에서는 칸이 18px, 글자가 하한(14px)에 걸려 칸의 77%를 채워 끼었습니다.
15칸이라 격자의 폭이 곧 글자 크기라 넓혀야 하는데,
격자만 밀어내면 라벨·버튼·채점표가 그대로 있어 **기둥이 두 개로 보입니다** —
「들은 문장을 써 보세요」가 격자보다 안쪽에서 시작하니 무엇을 가리키는 말인지 흐려지고,
오답노트 안에서는 격자가 카드 밖으로 삐져나옵니다.

「그냥 쓰기」는 넓히지 않습니다. 한 줄짜리 입력칸이 720px이면 눈이 되돌아오기 힘듭니다.

글자 크기는 화면(vw)이 아니라 **격자**를 따라갑니다(`container-type: inline-size` + `cqw`).
화면 폭으로 재면 같은 화면 안에서도 좁은 기둥 — 오답노트 카드 — 에서 글자가 칸에 꽉 낍니다.
어디서든 칸의 70~73%입니다.

| 화면 | 기둥 | 칸 | 글자 |
|---|---|---|---|
| 데스크탑·세로 태블릿 | 720 | 47 | 33 |
| 폰(세로) | 355 | 23 | 16 |
| 오답노트에서 풀 때 | 720 (카드째 넓힘) | 45 | 31 |

**채점표도 원고지로 그립니다** — `src/lib/grade-cells.ts`, `GradeSheet`의 `wongoji` prop.
격자 하나가 한 줄(15칸)이고 그 안에 위(내가 쓴 것)/아래(정답) 두 줄이 들어갑니다.
전체를 한 격자에 이어 붙이면 「내가 쓴 것」이 다 지나간 뒤에야 「정답」이 나와
위아래로 견주는 뜻이 사라집니다(절대 원칙 6).

**칸 세는 법이 쓰는 화면과 같아야 합니다.** 특히 쉼표 뒤 —
채점에 넘기는 문장에는 공백이 있어서 그냥 그리면 칸이 하나 더 생깁니다.
`absorbsFollowingSpace()`를 `toCells`와 함께 씁니다. 테스트가 내장 문제 전부를 지킵니다.

띄어쓰기 오류는 원고지에서 **칸 자체**로 드러납니다.
| 칸 | 윗줄 | 아랫줄 |
|---|---|---|
| `blank` 맞게 띄움 | 빈 칸 | 빈 칸(초록) |
| `needSpace` 안 띄움 | ∨ (빨강) | 빈 칸을 초록 테로 짚음 |
| `extraSpace` 더 띄움 | ✕ (빨강) | 정답에 없는 칸(점선) |
| `pad` 문장 끝난 뒤 | 남은 종이 | 남은 종이 |

### 인증과 프로필 — `src/proxy.ts`, `src/lib/profile*.ts`, `src/lib/pin.ts`
프록시(Next 16에서 middleware가 proxy로 바뀌었습니다)가 문 두 개를 지킵니다. **로그인했는가 → 누가 쓰는지 골랐는가.**
둘 중 하나라도 아니면 `/login` 또는 `/children`으로 보냅니다.
`/api/*`는 리다이렉트하지 않습니다. 화면이 아니라 fetch 응답이라, 각 라우트가 401 JSON을 돌려줍니다.

지금 누가 쓰는지는 쿠키 두 개로 기억합니다. **httpOnly라 화면 코드에서는 못 읽습니다.**
자녀 id가 필요한 화면에는 서버 컴포넌트가 props로 내려주세요 (`readActiveChild()`).

| 쿠키 | 뜻 |
|---|---|
| `bs_view` | `parent`(보호자 화면) / `child`(자녀 화면) / 없음(아직 안 고름) |
| `bs_child` | 마지막에 고른 자녀. 보호자 화면에서도 남습니다(리포트 기본값용) |

PIN은 `src/lib/pin.ts`의 scrypt로 **서버에서만** 다룹니다.
`children.pin_hash`는 부모 RLS로 읽히기 때문에, 화면에서 비교하면 해시가 브라우저로 내려갑니다.
그래서 `/api/children/[id]/enter`에서만 검증하고, 화면에는 `hasPin` 불리언만 보냅니다.
`PIN_LENGTH`·`isValidPin`은 `profile.ts`에 있습니다 — `pin.ts`는 node:crypto를 쓰므로 화면에서 불러올 수 없습니다.

보호자 화면 전환에는 잠금이 없습니다. 지침 9대로 화면을 감추는 정도로만 나눕니다.

### 복습 규칙 — `src/lib/review.ts`
`applySessionOutcomes()`가 별·졸업을 판정합니다. **세션을 끝까지 마쳤을 때만 호출하세요.**
같은 세션에 같은 문제가 여러 번 나와도 한 번만 반영합니다(하나라도 틀리면 틀림).

날짜는 별 판정에 쓰지 않습니다. `lastCorrectDate`는 지금도 기록하지만
"오늘 별을 받았어요" 같은 안내용입니다. `notesEligibleToday()`는 의미가 없어져 없앴습니다 —
아직 졸업하지 않았으면 언제든 별을 받을 수 있으므로 `activeNotes()`를 쓰세요.

### 음성 — `src/lib/tts.ts`, `tts-app.ts`, `tts-engines.ts`
층이 셋입니다. **화면은 어느 회사 소리를 쓰는지 모릅니다.**

```
화면  →  appSpeech (tts-app.ts)  →  /api/tts  →  tts-engines.ts  →  타입캐스트 / Google
                 ↓ 안 되면
           브라우저 내장 음성
```

**공급자는 환경 변수가 정합니다.** `TYPECAST_API_KEY`가 있으면 타입캐스트, 없으면 Google.
**키를 지우면 곧바로 되돌아갑니다** — 회사별 차이는 `tts-engines.ts` 한 파일에 갇혀 있습니다.

**한 회사가 막히면 다음 회사로 넘어갑니다**(`pickEngines()`가 목록을 돌려줍니다).
아이 화면에서 **소리가 나는 것**이 어느 회사 소리냐보다 중요합니다.
예전에는 회사 하나만 고르고 끝이라, 타입캐스트가 403으로 막히자
Google 키가 멀쩡한데도 브라우저 내장 음성으로 떨어져 며칠이 그대로 갔습니다.
막힌 회사는 10분간 건너뜁니다(`markBlocked`) — 문장마다 헛걸음하면 아이가 그만큼 기다립니다.

**목소리는 회사가 무엇이든 여자·남자 둘만** 냅니다(`TYPECAST_VOICES`, `GOOGLE_VOICES`).
회사가 바뀌었다고 설정 화면이 둘에서 마흔하나로 늘어나면 안 됩니다.
`matchVoice()`가 **남녀를 지켜서 옮깁니다** — 안 그러면 「남자 목소리」로 해 둔 집이
어느 날 말없이 여자 목소리를 듣게 됩니다.

회사를 고른 자취:
- 클로바 → 월 고정비 9만 원. 우리 사용량으로는 그 돈의 1%도 못 씁니다.
- OpenAI → 목소리가 영어에 맞춰져 있어 한국어 받침이 흐려집니다.
- Google → 종량제라 좋았지만 **발음이 또렷하지 않았습니다.** 「송골」이 「쏭골」로 들렸습니다.
- 타입캐스트 → 한국어 전용. 들어 보고 골랐습니다. 대신 MP3가 **Google의 8배 크기**입니다.

회사마다 다른 것 — 새 회사를 붙일 때 여기부터 보세요:
- **쉼**: Google은 SSML `<break>`, 타입캐스트는 **그런 게 없어 쉼표를 끼워 넣습니다.**
- **글자 수**: Google은 SSML 태그까지 셉니다. 타입캐스트는 쉼표까지 셉니다.
- **목소리 목록**: Google은 언어로 걸러 주지만 **타입캐스트는 언어 정보를 아예 주지 않습니다.**
  1,125개 중 한국어를 가릴 방법이 없어 손으로 고른 목록을 씁니다(`TYPECAST_VOICES`).
- **기본 목소리**: 회사마다 형태가 달라(`ko-KR-…` vs `tc_…`) **서버가 정해서 알려 줍니다.**
  다른 회사의 이름이 기기에 남아 있어도 형태가 안 맞으면 조용히 기본값으로 돌아갑니다.

**폴백은 아이에게 조용히, 부모에게는 알립니다.** 아이 화면에 "음성 오류"가 뜨면 문제 풀다 멈춥니다.
대신 넘어간 사실을 기기에 적어 두고 보호자 설정에서 보여 줍니다(`TtsStatus`).
그러지 않으면 **며칠째 기기 목소리로 읽고 있어도 아무도 모릅니다.**

문장별로 브라우저 메모리에 소리를 담아 둡니다(`tts-app.ts`의 캐시, 80개).
**이 캐시가 "다시 듣기는 매번 같아야 한다"를 이미 보장합니다** — 한 세션은 화면 하나라
담아 둔 소리를 그대로 다시 틉니다. 생성형 목소리를 쓸 수 있는 이유가 이것입니다.

읽기 방식 3종: `flow`(이어 읽기) / `chunked`(어절마다 쉼) / `teacher`(전체→끊어서→전체)
**`chunked`는 낱말을 따로 합성하지 않습니다.** 그러면 TTS가 낱말마다 문장을 끝내듯 읽어
또박또박 들으려던 것이 오히려 더 안 들립니다. 한 번에 보내고 사이만 벌립니다.

**기본 속도는 1.0(보통)입니다.** 늦추면 소리를 늘려 재생해서 마찰음이 길어지고,
귀는 그걸 된소리로 듣습니다 — 「천천히」가 오히려 덜 또렷했습니다.
**속도만 세션 화면에 둡니다.** 문장마다 「이건 잘 안 들려」가 있으니까요.
쓰기 방법·목소리는 보호자 설정에만 둡니다 — 한 번 정하면 그만인 것들입니다.

쓰기 방법을 세션에서 뺀 것은 취향이 아니라 **채점이 뒤집히기 때문**입니다.
원고지 모드에서만 쉼표 규칙을 문장으로 되돌리므로, 규칙대로 「마시고,빵도」라고 써 놓고
그냥 쓰기로 바꾸면 글자는 그대로인데 띄어쓰기를 틀렸다고 나옵니다.

### 오답을 볼 틈 — `src/components/NextButton.tsx`
채점 결과에서 **틀렸을 때만** 「다음」을 2초 잠급니다. 남은 초를 버튼에 보여 줍니다.

아이는 「다음」 자리를 손가락으로 기억해서, 채점 화면이 뜨는 순간 이미 누르고 있습니다.
정답이 화면에 떠 있었는데도 못 보고 지나갑니다.
맞았을 때는 잠그지 않습니다 — 상 주는 자리가 벌 서는 자리가 되면 안 됩니다.
남은 초를 감추면 아이는 고장인 줄 알고 더 세게 누릅니다.

### 효과음 — `src/lib/sfx.ts`
음원 파일 없이 Web Audio로 생성합니다. 짧고 부드럽게 유지하세요.
오답은 벌 주는 소리가 아니라 주의를 옮기는 신호 정도여야 합니다.

### 디자인 시스템 — `src/app/globals.css`
소재는 원고지(국어 공책의 격자), 빨간펜 교정, 연필 흑연입니다.

**색마다 역할이 하나씩입니다.**
- 초록(`--grid`) = 구조와 정답
- 빨강(`--pen`) = 교정
- 금색(`--gold`) = 보상. **보상 화면과 별 표시 외에는 쓰지 마세요.**
  금색이 흔해지면 보상의 의미가 사라집니다.

표제는 명조(`Gowun Batang`, `.display` 클래스), 본문은 Pretendard입니다.
`.grid-cell` 계열이 원고지 칸이고, 이게 이 앱의 시그니처입니다.

---

### 세션 저장 — `src/app/api/sessions/route.ts`
**기록·오답노트·보상이 한 라우트에서 한 번에 일어납니다.**
세션을 끝까지 마쳤을 때만 부릅니다(절대 원칙 2). 중간에 나가면 아무것도 보내지 않습니다.

- 점수는 화면이 보낸 값을 쓰지 않고 서버가 `scoreOf()`로 다시 계산합니다.
- `logAttempt: false`는 오답노트에서 한 문제만 그 자리에서 풀 때만 씁니다.
  한 문제짜리 0점/100점이 쌓이면 리포트의 평균 점수가 흔들리기 때문입니다.
  이때도 별·졸업은 그대로 반영됩니다.
- 날짜는 언제나 `toDateKeyInSeoul()`·`seoulWeekStart()`로 셉니다.
  서버가 UTC로 돌면(Vercel) 아침 기록이 어제로 밀려 리포트의 "오늘"과 "이번 주"가 어긋납니다.
- 보상은 `trophyFor(score, mode)`가 정합니다. 연습 모드는 언제나 null입니다.

화면 쪽은 `saveSession()`(`src/lib/save-session.ts`)으로 부르고,
**실패해도 결과를 손에 쥔 채 다시 보낼 수 있게** 해 두었습니다.
끝까지 푼 아이에게 "처음부터 다시"라고 말하지 않기 위해서입니다.

### 화면 구성 요약

| 경로 | 누가 | 하는 일 |
|---|---|---|
| `/dictation` → `/dictation/[id]` → `/[id]/play?mode=` | 자녀 | 세트 고르기 → 연습/시험 → 세션 |
| `/dictation/new`, `/dictation/[id]/edit` | 보호자 | 세트 만들기·고치기 (사진 인식 포함) |
| `/spelling` → `/spelling/[kind]?mode=` | 자녀 | 문제은행에서 10문제, 서버에서 섞음 |
| `/notes` | 자녀 | 오답노트. **항목을 그 자리에서 펼쳐 풉니다** (`InlineNotePractice`) |
| `/notes/practice?module=` | 자녀 | 쌓인 오답을 몰아서 풀기. 언제나 연습 모드(별만, 배지 없음) |
| `/report?child=` | 보호자 | 주차별 평균, 오답 유형, 최근 기록 |
| `/trophies` | 둘 다 | 보관함 |
| `/more` | 둘 다 | 묶음 메뉴. **아이는 여기서 효과음만 바꿉니다** |
| `/settings` | **보호자만** | 아이가 푸는 방식(속도·목소리·쓰기 방법·효과음) · 집 관리(프로필·잠금·계정) |
| `/auth/confirmed` | — | 가입 메일 링크가 닿는 자리. **아무것도 하지 않습니다** |

`/auth/confirmed`가 비어 있는 것은 의도입니다.
메일은 대개 **다른 기기**에서 열리는데, 거기서 로그인 세션까지 만들려면
가입할 때 그 브라우저에 넣어 둔 열쇠가 필요합니다. 폰에는 없으니 실패합니다.
확인은 링크를 누르는 순간 Supabase에서 끝나므로, 우리는 "끝났어요"만 보여 주면 됩니다.

## 내장 문제은행 — `src/data/dictation-bank.ts`, `src/data/spelling-bank.ts`

**받아쓰기**는 학년·학기 표시가 없습니다. **1단계부터 20단계**로만 올라갑니다.
「급」이 아니라 「단계」인 이유: **학교 문제지가 「급」을 씁니다.**
사진으로 넣은 학교 문제와 내장 문제가 목록에 나란히 놓이는데 둘 다 「3급」이면 아이가 헷갈립니다.
목록에서도 **직접 넣은 문제가 위**입니다 — 학교에서 받아온 것이 오늘 할 일이니까요.

실제 학교 받아쓰기 자료(1학년 2학기용)를 열어 난이도 진행 순서만 참고했고,
낱말·문장은 전부 새로 썼습니다(저작권 있는 원문을 그대로 옮기지 않습니다).
id는 `lv1`~`lv20` 형태이고 `attempts.builtin_set_id`에 그대로 남습니다 —
**한 번 배포한 id는 바꾸지 마세요.** 바꾸면 아이가 쌓은 점수와 이어지지 않습니다.

**문장을 고치는 것도 조심하세요.** 받아쓰기 오답노트는 `refId`로 **문장 원문 자체**를 씁니다.
문장을 바꾸면 그 문장에 걸린 오답노트가 끊깁니다.
반면 맞춤법은 문항 id를 쓰므로 문제·보기·설명을 고쳐도 이어집니다.
**단계 이름과 `focus`는 표시용이라 언제 바꿔도 안전합니다.**

**20단계를 뺀 모든 문장은 원고지 한 줄(15칸)에 들어갑니다.** 테스트가 지킵니다.
20단계만 예외입니다 — 거기는 두 문장을 이어 받아쓰는 것이 목적이라
칸에 맞추려고 줄이면 마지막 단계가 마지막 단계가 아니게 됩니다.

받아쓰기 문장에는 ㅐ/ㅔ가 모두 실제 낱말인 경우(개/게, 새/세, 배/베)를 홀로 쓰지 않습니다.
소리만으로는 정답을 정할 수 없기 때문입니다. 대신 그런 낱말은 **맞춤법** 쪽에서
문맥(문장)과 함께 다룹니다 — `개/게`, `새/세`, `배/베` 태그로 들어 있습니다.

**맞춤법**은 76문항(mcq 47 / fill 11 / find 18)이고 `level`(basic/inter/advanced)이 붙어 있습니다.
`pickBalanced()`가 세션마다 기본·중급을 많이, 고급을 조금만 섞어서 냅니다.
완전 무작위(`pickQuestions()`)로 뽑으면 저학년이 고급 문항을 몰아 받아
시험 점수가 실력과 무관하게 갈릴 수 있어서, 세션 화면은 반드시 `pickBalanced()`를 씁니다.
level은 지금 데이터에만 있고 화면에서 난이도를 고르는 기능은 아직 없습니다.

## 지금 어디까지 왔나

**배포되어 실제로 쓰고 있습니다.** 화면·콘텐츠·개인정보처리방침·카카오 로그인 다 끝났습니다.

| | |
|---|---|
| 주소 | badasseugi-wine.vercel.app |
| 앱 서버 | Vercel 서울(`icn1`) |
| 데이터베이스 | Supabase 도쿄(`ap-northeast-1`) — **리전은 나중에 못 바꿉니다** |
| 음성 | 타입캐스트 (키를 지우면 Google로) |

### 손대면 좋을 것 (급하지 않음)

- **회원가입 오류 메시지가 원인을 안 가립니다.** 지금은 무엇이 틀렸든 한 문구만 뜹니다.
- **안 쓰는 `spelling_bank` 테이블.** 맞춤법 문제는 코드에 있어서 이 테이블은 비어 있습니다.
- **화면당 데이터 조회 횟수.** 리전을 서울로 옮겨 많이 나아졌으니 재 보고 판단하세요.
- **원고지 입력 2단계.** 지금은 자동완성이 그대로 뜹니다.
  글자를 즉시 거둬들여 입력칸을 비우면 막히는데, 키보드 앱마다 동작이 달라 실기기 확인이 필요합니다.

### 환경 변수

`.env.example`이 단일 출처입니다. 여기서는 **판단이 달린 것만** 적습니다.

| 변수 | 없으면 |
|---|---|
| `TYPECAST_API_KEY` | Google로 읽습니다. **지우는 것이 곧 되돌리기입니다** |
| `GOOGLE_TTS_API_KEY` | 둘 다 없으면 브라우저 내장 음성 |
| `TTS_DAILY_LIMIT` | 타입캐스트 500 / Google 20,000 (회사마다 기본이 다릅니다) |
| `TYPECAST_VOICE_ID` | `TYPECAST_VOICES`의 첫 번째 |

**서비스 롤 키는 일부러 두지 않습니다.** 넣으면 RLS를 통째로 우회해 가족별 분리가 무너집니다.

타입캐스트 무료는 **월 15,000자**입니다. 한 세션이 200자쯤이라 하루 500자로 잡아 두었습니다.
**타입캐스트일 때는 `TTS_DAILY_LIMIT`으로도 500자 위로 못 올립니다**(`TYPECAST_MAX_DAILY`).
한 변수를 두 회사가 나눠 쓰는데 감당하는 양이 40배 달라서, Google 기준(20,000)을 넣어 둔 채
타입캐스트로 바꾸면 월 한도를 하루에 넘겨 씁니다. **실제로 그렇게 막혔습니다** —
합성이 403 `UNUSUAL_ACTIVITY_DETECTED`를 받았고 목소리 목록(200)은 멀쩡했습니다.
막힌 것은 `tts-blocked`로 갈라서 보호자에게 "기다려도 안 풀린다"고 알립니다.
Google은 월 100만 자까지 무료라 훨씬 넉넉합니다.

### 알아 두면 헤매지 않을 것

- **파이썬과 LibreOffice가 이 PC에 없습니다.** 파이썬은 스토어 별칭뿐이라 조용히 아무것도 안 합니다.
  문서·엑셀을 만들 일이 있으면 Node(`exceljs`, `docx`)를 쓰세요.
- **`perl -0777 -pe`로 여러 줄을 치환하면 파일이 깨진 적이 여러 번 있습니다.**
  소스 편집은 Edit 도구나 Node 스크립트를 쓰세요.
- **개발 서버가 켜져 있을 때 `npm run build`를 돌리면 서로 `.next`를 물어 죽습니다.**

---

## 기술 스택

| 영역 | 선택 |
|---|---|
| 프레임워크 | Next.js 15 (App Router) + TypeScript strict |
| 스타일 | Tailwind + CSS 변수 (`globals.css`가 단일 출처) |
| 백엔드 | Supabase (Postgres, Auth, Storage) |
| 인증 | 이메일 + 카카오 |
| 테스트 | Vitest |
| 배포 | Vercel |

---

## 개발 명령

```bash
npm install
npm run dev          # 개발 서버
npm test             # 테스트 (채점 로직 수정 시 필수)
npm run test:watch   # 테스트 감시 모드
npx tsc --noEmit     # 타입 검사
```

## 설정 순서

1. `.env.example`을 `.env.local`로 복사하고 값을 채웁니다
2. Supabase 프로젝트를 만들고 `supabase/migrations/`의 파일을 **번호 순서대로 모두** 실행합니다
   (`0001_init` → `0002_parent_pin` → `0003_tts_usage` → `0004_builtin_sets` → `0005_drop_spelling_bank`).
   이미 돌아가고 있는 프로젝트라면 아직 안 돌린 번호만 실행하면 됩니다.
3. Supabase 대시보드 > Authentication 설정
   - **URL Configuration > Redirect URLs**에 아래를 등록합니다. 여기에 없는 주소로는 되돌아오지 못합니다.
     `http://localhost:3000/auth/callback`, `https://<배포주소>/auth/callback`
   - **Providers > Kakao**를 켜고 카카오 개발자센터의 REST API 키와 Client Secret을 넣습니다.
     카카오 쪽 Redirect URI에는 Supabase가 알려주는 `https://<project>.supabase.co/auth/v1/callback`을 넣습니다
     (우리 앱 주소가 아닙니다).
   - 이메일 확인을 켜 두면 가입 직후 세션이 없습니다. 화면은 "메일함을 확인해 주세요"로 안내합니다.
4. `npm run dev`

---

## 코드 작성 시 지켜야 할 것

- **한국어 주석을 답니다.** 이 프로젝트는 한국어 학습 도메인이라 용어를 한국어로 유지하는 게 명확합니다.
  다만 "무엇을 하는지"가 아니라 **"왜 그렇게 했는지"**를 씁니다.
- **UI 문구는 아이가 읽을 수 있게.** 어려운 한자어를 피하고, 동작 이름은 화면 전체에서 통일합니다.
  ("제출하기" 버튼을 눌렀으면 결과에도 "제출"이라는 말을 씁니다.)
- **실패 화면과 빈 화면은 다음 행동을 제안합니다.** 상태만 알리고 끝내지 않습니다.
- **접근성 기본선**: 키보드 포커스가 보이게, 모바일까지 반응형, `prefers-reduced-motion` 존중.
  이건 이미 `globals.css`에 들어가 있으니 깨뜨리지만 마세요.
